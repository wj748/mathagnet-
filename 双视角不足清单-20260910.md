# 双视角不足清单（工程师 / 用户）

> 审查日期：2026-09-10　|　方式：两条线并行静态审查 + 关键项人工核实代码
> 范围：**排除** `隐患审查报告-20260910.md` 第七节已修复项（新板块路由、判分单位、safety 归一化、OCR 脱敏、出题去重、监控鉴权、异常收敛）。
> 本清单聚焦**尚未解决**的深层不足：工程师视角 20 条、用户视角 19 条，均已回到源码核对行号。

---

## 一、工程师视角（能不能长期维护、扩展、稳定运行）

### 1. 可靠性：数据会静默丢失（最高优先）

| 位置 | 问题 | 影响 |
|---|---|---|
| `graph/nodes.py:154-155` | `record_attempt` 失败 `except Exception: pass` | **答题记录是习惯追踪的唯一数据源**，写失败无声无息，错题报告与长期记忆长期失真 |
| `graph/nodes.py:201-202` | `create_session` 失败 `except: pass` | 会话建不上但流程照常走，后续交卷无归属 |
| `graph/nodes.py:278-279` | `finish_session` 失败 `except: pass` | 同上 |

这三处是同一类病：**静默失败**。建议统一改为 `log.exception` + 有限重试，并把落库失败计入 metrics（现在 metrics 里根本没有落库失败这一项，等于没有告警）。

| 位置 | 问题 |
|---|---|
| `graph/builder.py:75-84` | `InMemorySaver` checkpointer：进程重启，全部进行中 quiz/learn 会话丢失 |
| `metrics.py` / `trace.py` | 观测数据纯内存环形缓冲，重启归零，无法做跨重启趋势与告警 |
| `memory/short_term.py` | Redis 可选依赖，未配置时默认内存兜底 → 同样重启即失 |

### 2. 测试：回归防护形同虚设 ⚠ 最被低估的一条

`scripts/eval_optimizations.py:32-35`：

```python
def check(name, cond, detail="") -> bool:
    RESULTS.append((name, bool(cond), detail))
    print(f"{'✔' if cond else '✘'} {name}")   # 只打印，不 assert
    return bool(cond)
```

**后果**：这些 `test_*` 函数被 pytest 收集后，**即便全部断言失败也会判 PASS**（函数正常返回、无异常抛出）。所谓"七套验收全绿"在 CI 语境下是**假绿**——只有人肉看终端输出才发现得了。

建议：`check` 内失败即 `raise AssertionError`，或 test 末尾 `assert all(...)` 汇总。这一行改动就能让七套评测具备真实门禁能力。

配套问题：`tests/` 仅 7 个冒烟用例，无 conftest、无 LLM mock，核心路径（判分、路由、记忆隔离）无单元测试。

### 3. 架构：板块命名三套体系并存，无单一真源

| 处 | 词汇表 |
|---|---|
| `teaching.py` CURRICULUM（讲解口径） | 数与式 / 方程与不等式 / 函数 / 几何 / 统计与概率 / 锐角三角函数（**6 个**） |
| `tools/quiz.py:15` TOPIC_ENUM（题库口径） | 函数 / 二元一次方程 / 几何图像 / 综合（**4 个**） |
| `graph/nodes.py:27` `_NO_BANK_TOPICS` | 教学→题库的兜底映射（统计与概率→综合、锐角三角函数→综合） |

昨天扩容知识点时正是踩了这个坑（只改教学侧、漏改刷题侧，产生一条 P0）。现在是靠 `_NO_BANK_TOPICS` 打补丁维持，**每加一个板块要同步改三处**，且无校验。建议抽 `TOPIC_REGISTRY` 单一真源 + 枚举校验。

### 4. 性能与并发

- `api/server.py:173-184`：SSE **伪流式**——整轮 `invoke` 算完再逐字 yield，首字节延迟 = 整轮耗时（LLM 在线时 20-30s），且无 `request.is_disconnected()` 检测，客户端关页面 LLM 仍跑完。
- `llm.py:127`：单次 60s 超时，但**无重试、无整轮总预算**；一轮最坏触发路由 + ≤8 次 ReAct + 判分 + 分析多次调用，弱网/限流下无退避恢复。

### 5. 数据质量

- `teaching.py:421-427` `find_point_index`：`kp["name"] in text` 子串匹配 → **"分式" ⊂ "分式方程"** 会误命中错误知识点，讲错对象。
- 题库 2034 题中模板生成 373 题（18%），难度标注由源数据继承、无校验；**答案正确性无抽样校验机制**。
- `vectorstore.py:158-167` `search_knowledge` 未应用 `score_threshold`（而 `memory_tools.py` 用了 0.10），低相关片段会进讲解。

### 6. 配置与部署

- 无 `Dockerfile`（`docker-compose.yml` 只起 Redis）、无 `.github` CI、无 `.env.example`。
- `requirements.txt` 全 `>=` 未锁版本（`langgraph>=0.2`、`qdrant-client>=1.9`、`transformers>=4.40`），上游破坏性更新会悄然生效。
- 模型名只能经 `GLM_MODEL` 环境变量注入，`config.yaml` 无 `llm.model` 字段。
- 无数据库迁移脚本（SQLite schema 随代码演进，升级需手工改库）。

### 7. 代码质量

- 魔法数字散落：`nodes.py:54` 题量上限 20 vs `quiz.py:22` `le=50` 不一致；检索阈值 0.10 多处重复。
- `nodes.py:219` `grading_node` 内 `import re as _re` 且重复实现路径正则（与 ocr 工具重复）。
- 无 ADR / 设计决策记录，硬约束纪律只存在于 `技术文档.md` 与记忆文件里。

### 8. 文档漂移

`README.md:251` 仍描述 `CURRICULUM` 为"函数 6 / 二元一次方程 4 / 几何图像 5"，实际已是 6 个一级板块。建议文档由代码生成或加一致性测试。

---

## 二、用户视角（学生 / 家长 / 老师用起来顺不顺）

### 1. 连续学习：刷新即失忆 ⚠ 致命

`web/index.html:251`：

```js
const USER = 'u_web_' + Math.floor(Math.random()*100000);
```

**每次刷新页面都是全新身份**。刷题到一半刷新 → 题目、作答、错题、学习进度全部丢失且无法恢复（服务端会话也在内存 `last_state`）。对一个"学到一半手滑刷新"的初中生，这是**劝退级**体验。

建议：`localStorage` 持久化 `user_id`（5 行代码）+ 服务端持久化 checkpointer。

### 2. 教学深度：只判对错，不教方法 ⚠ 违背"辅导"定位

`graph/nodes.py:159`：

```python
head = "✅ 回答正确！" if out["correct"] else "❌ 回答错误。"
```

答错后**直接进下一题**，没有任何方法引导。想看解析必须**主动**输入"解析/讲解/为什么/怎么做/提示"（`nodes.py:93`）——但大多数孩子不知道要打这两个字，等于只判对错。

雪上加霜：`nodes.py:97` 在题库 `analysis` 为空时返回 `💡 本题解析：（该题暂无解析）`，生硬收场。

建议：答错后**主动**给一条分层提示（先给方向 → 再给关键步骤 → 最后揭晓），并保证每题有兜底解析（无 analysis 时用"读题→找条件→选方法→验证"通用模板）。

同样问题在 `knowledge_tutor.py:137-143`：学习小测答错立即给标准答案，无"再试一次"，容易直接抄答案。

### 3. 拍照批改：失败被静默成"已上传" ⚠ 主打功能实际可用率低

`web/index.html:471-473`：

```js
const r = await fetch('/api/upload?...');
const j = await r.json();
bubbleAI(j.reply || '已上传');     // 不检查 r.ok
```

- 上传非图片 / 超 10MB / **iPhone 默认 HEIC 格式**（`ocr.py:30` 白名单只有 png/jpg/jpeg/bmp/webp/pdf/txt/md）→ 服务端 413/500，前端**照样显示"已上传"**，学生以为批改完成却什么都没有。
- 上传时断网：`fetch` 未包 try/catch，点了没反应。
- OCR 低置信度只能"确认"或重拍，**无法就地编辑识别文本**。

### 4. 等待体验

LLM 在线时一次对话 20-30s，`index.html:391` 只有静态 `'…'`：无"正在思考"文案、无动画、无阶段提示（读题/批改），学生会以为卡死而反复点击或刷新（刷新正好触发 §1 的丢进度）。

状态徽章显示"离线规则模式"（`index.html:164`）——让初中生以为功能残缺，建议改"AI 老师在线 / 基础模式"。

### 5. 界面门槛：调试术语直接暴露给 12 岁用户

右侧栏直接展示「本轮 Trace」「（LangGraph）」「运行状态：向量后端 / 题库{…}」。学生看不懂"Trace/意图/向量后端"，会降低信任。建议默认折叠，或改名"我的学习数据"，仅开发者模式可见。

### 6. 移动端

`@media(max-width:900px){.grid{grid-template-columns:1fr}}` —— 窄屏下右侧栏堆叠到聊天区**下方**，"上传作业照片"入口被埋到需要下滑才能看到。手机拍照批改是高频场景，建议上传按钮改为底部常驻悬浮。

（符号栏 `flex-wrap` 正常换行，未溢出，该项 OK。）

### 7. 无障碍

对错主要靠红绿文字 + ✅❌ 表情传达，**红绿色盲学生难以分辨**。建议增加形状/边框双重标识。

### 8. 激励与坚持

全程无连续天数、积分、徽章、成就等任何正反馈。初中生难坚持。

### 9. 家长 / 老师视角

- 学习习惯报告（`nodes.py:306-372`）只在聊天里纯文本呈现：仅累计题数/正确率/板块正确率，**无时间趋势、无薄弱知识点排行、不可导出**。家长看不到"这周进步了吗"。
- 无多学生/班级管理、无家长视图、无老师"布置练习"入口。身份是随机字符串，无法区分谁是谁。

### 10. 信任与合规感知

`safety.py` 已做输入审核、PII 脱敏、输出净化（做得不错），但**界面从未向未成年人及家长告知**隐私保护与内容边界，也无家长同意声明。家长担心孩子数据安全时看不到任何说明。

---

## 三、两视角的交汇：三个共同的根因

| 根因 | 工程师看到 | 用户看到 |
|---|---|---|
| **状态不持久** | InMemorySaver / 内存 metrics / 内存 short_term | 刷新丢进度、跨天无历史、家长看不到趋势 |
| **静默失败** | 三处 `except: pass` + check() 不 assert | 上传失败显示"已上传"、无解析时给一句空话 |
| **教学深度不足** | 解析依赖题库 `analysis` 字段、无兜底 | 答错只判对错、想学方法不知从哪开口 |

这三条不是独立问题——**修一个能同时消掉两边的症状**，应最优先。

---

## 四、优先级路线图

### 立即做（低成本、零风险、收益高，合计约 1 小时）

1. `check()` 加 `assert` —— 让七套评测具备真实门禁能力（1 行）
2. `nodes.py` 三处 `except: pass` → `log.exception` + metrics 计数（3 处）
3. `index.html` `USER` 改 `localStorage` 持久化（5 行）
4. `index.html` 上传检查 `r.ok` + try/catch + HEIC 友好提示
5. `README.md:251` 板块描述修正

### 短期（1-2 天）

6. 答错分层提示 + 解析兜底模板（教学核心体验）
7. 持久化 checkpointer（SqliteSaver）+ trace/metrics 落 SQLite
8. 等待态：动态"AI 正在思考…" + 阶段提示 + 超 30s 友好提示
9. 移动端上传入口悬浮 + 调试面板默认折叠
10. 状态文案改为"AI 老师在线 / 基础模式"

### 中期（需要设计决策）

11. 抽 `TOPIC_REGISTRY` 单一真源，消除三套板块命名
12. `Dockerfile` + 依赖锁版本 + GitHub Actions CI
13. SSE 真流式（LLM token 级）+ 断连检测；LLM 重试与整轮超时
14. `find_point_index` 精确匹配优先，避免子串误命中
15. 家长/老师视图：可导出报告（趋势 + 薄弱点 TOP）、轻量账号绑定
16. 无障碍：对错双重标识；隐私说明入口

---

## 五、一句话结论

**工程师视角最该先做的三件事**：评测真断言化（否则所有"全绿"都不可信）、消灭三处静默吞异常、统一板块命名单一真源。

**用户视角最该先做的三件事**：持久化用户身份（否则刷新劝退）、答错要教方法而不是只判对错、上传失败别再显示"已上传"。

两边指向同一个起点：**先把"静默"和"不持久"这两类病根治掉**——它们成本低、影响广，且修完同时改善工程质量与用户体验。

---

## 六、实施完成记录（2026-09-10 全部落地）

**立即做 5 项（1-5）**：✅ 全部完成（eval 真断言门禁 / except:pass→log+metrics /
USER localStorage / 上传 r.ok+HEIC 引导 / README 修正）。

**短期（6-10）**：✅ 全部完成。
- 分层提示 `_hint_for(q, level)`（方向→前两句解析→揭晓答案）+ 解析空时兜底步骤模板；
- SqliteSaver 当前版本不可用 → 改自研落盘（QuizSession/AgentState/TraceTurn 表，
  重启恢复验证通过）；
- 呼吸点等待态 + FAB 悬浮上传（选完自动上传）+ 调试面板折叠 + 状态文案；
- 新增 `POST /api/reset`（清 attempts/sessions/agent_states + 内存 Agent 弹池）
  对应隐私说明「重置我的学习数据」承诺。

**中期（11/12/14）**：✅ 完成。
- `src/math_agent/topics.py` 单一真源：BANK_TOPICS / TEACHING_TO_BANK / to_bank_topic，
  消除 _EXAMPLE_TOPIC 与 _NO_BANK_TOPICS 的矛盾映射（锐角三角函数实测回退综合）；
  quiz.py TOPIC_ENUM、repository.TOPICS 均改为引用；MAX_COUNT=20 对齐两处题量上限；
- `find_point_index` 两遍最长匹配（完整名 → 片段），"分式"⊂"分式方程"劫持修复；
- `search_knowledge` 内置 `retrieval.score_threshold`（默认 0.10，config 统一）；
- requirements.lock.txt（108 项 pin）/ Dockerfile（HOST/PORT env + HEALTHCHECK）/
  .github/workflows/ci.yml（pytest + eval_memory/teaching 门禁）/
  docker-compose app 服务（profile full）。注：.env.example 早已存在，清单该项过时。

**回归结果（2026-09-10 15:30，离线规则模式）**：
判分 34/34、路由 11/11、记忆 21/21、安全 16/16（修正一条永假断言的 eval 自身 bug）、
教学 19/19、badcase 46/46（新增 43-46 板块真源回归）、pytest 7 passed；
端到端：health / session / SSE 刷题 / stats / reset 全通过，服务已重启。

**遗留（13/15/16，需更大改动，未列入本轮）**：SSE 真流式 + LLM 重试预算；
家长/老师视图导出；无障碍对错双重标识（对错已有 ✓/❌ 符号 + 颜色，视需求再增强）。
