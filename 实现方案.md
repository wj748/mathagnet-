好的！我已将完整方案整理为 .md 文档，并新增了 从 0 到 1 的落地步骤 章节。你可将下方代码块中的内容保存为 math_agent_design.md：
# 初中数学辅导 Agent 落地方案（基于 LangGraph）
> 版本：v1.0
> 技术栈：LangGraph + LangChain + 向量数据库（Milvus/Qdrant）+ LLM
> 更新日期：2026-09-07
---
## 目录
1. [项目概述](#一项目概述)
2. [LangGraph 整体架构设计](#二langgraph-整体架构设计)
3. [核心功能模块设计](#三核心功能模块设计)
4. [优化策略实现方案](#四优化策略实现方案)
5. [System Prompt 设计与解析](#五system-prompt-设计与解析)
6. [从 0 到 1 落地步骤](#六从-0-到-1-落地步骤)
7. [风险与应对](#七风险与应对)
---
## 一、项目概述
### 1.1 产品定位
面向初中学生（或家长）的数学辅导 Agent，核心能力：
- **刷题**：支持选择题、填空题、应用题；可按板块（函数、二元一次方程、几何图像）练习
- **错题分析**：在用户完成一次完整刷题（或上传成绩单）后，统一分析错题类型并生成报告（非逐题反馈）
- **作业批改**：支持应用内作答与外部上传（PDF/图片）
- **习惯追踪**：记录用户做题习惯，分析优缺点并分享
### 1.2 技术选型总览
| 模块 | 选型 | 说明 |
|------|------|------|
| Agent 编排 | LangGraph | 状态机式工作流，支持条件边、循环控制 |
| LLM | GLM 系列 | 推理 + 工具调用 |
| 向量数据库 | Qdrant / Milvus | 双 Collection：知识库 / 用户长期记忆 |
| Embedding | BGE / text-embedding 系列 | 用于检索与信息增益计算 |
| OCR | PaddleOCR / 多模态 LLM | PDF、图片解析 |
| 业务数据库 | MySQL / PostgreSQL | 题库、答题记录、用户信息 |
| 缓存 | Redis | 会话短期记忆、防重调用 hash |
---
## 二、LangGraph 整体架构设计
### 2.1 状态定义（State）
```python
from typing import TypedDict, List, Optional
from langchain_core.messages import BaseMessage
class MathAgentState(TypedDict):
    messages: List[BaseMessage]              # 全局对话历史
    user_id: str                             # 用户标识
    intent: str                              # 意图识别结果 (刷题/批改/习惯查询/闲聊)
    quiz_session: dict                       # 当前刷题会话 (题目列表、进度、答题记录)
    short_term_memory: dict                  # 短期记忆 (当前上下文、待处理的文件解析数据)
    react_loop_count: int                    # ReAct 循环计数器
    last_result_vector: Optional[List[float]] # 上一次思考结果向量 (信息增益计算)
    tool_call_history: List[dict]            # 工具调用历史 (防重复调用)
```
### 2.2 核心节点与流转图
```
用户输入
   │
   ▼
[Intent Router 意图路由]
   ├── 刷题 ──► [Quiz Manager] ──► 用户作答 ──► 判题 ──► 累积错题
   │                                                  │
   ├── 批改 ──► [OCR] ──► [Assignment Grader] ───────┤
   │                                                  │
   ├── 习惯查询 ──► [Memory Manager 检索长期记忆] ──► 输出习惯报告
   │                                                  │
   └── 闲聊/答疑 ──► [ReAct Executor] ◄───────────────┘
                        │
                        ▼
                [Error Analyzer 错题分析]
                （仅在"交卷"或批改完成时触发）
                        │
                        ▼
                [Memory Manager 写入长期记忆]
                        │
                        ▼
                    输出报告
```
| 节点 | 职责 |
|------|------|
| **Intent Router** | 解析用户意图，路由到业务节点或 ReAct 循环 |
| **Quiz Manager** | 按板块/题型拉题，维护 quiz_session 状态 |
| **Assignment Grader** | OCR 解析 + 数学判题，返回批改结果 |
| **Error Analyzer** | 汇总错题，生成错题报告与修改建议 |
| **ReAct Executor** | 核心 Agent 循环，调用工具（检索、计算器等） |
| **Memory Manager** | 短期→长期记忆提取、习惯分析写入 |
---
## 三、核心功能模块设计
### 3.1 刷题模块
- `fetch_quiz(topic, question_type, count, difficulty)` 工具从题库拉题
- `quiz_session` 维护：题目列表、当前索引、用户答案、对错标记
- 支持用户切换板块、跳题、查看单题解析（即时反馈仅提示对错，不给报告）
### 3.2 错题分析模块
- **触发条件**（满足其一）：
  - 用户输入"交卷"、"结束刷题"、"生成报告"
  - 成绩单批改流程完成
- **分析流程**：汇总本次全部错题 → LLM 归类错因（概念不清 / 计算错误 / 审题偏差 / 方法缺失）→ 输出结构化报告
- **报告结构**：错题总览 → 错因分类占比 → 逐题分析 → 修改建议 → 针对性练习推荐
### 3.3 作业批改模块
- 图片/PDF → OCR 工具提取文本与手写内容 → 结构化为题目+答案
- 调用 Math Eval 工具判定对错，LLM 处理过程分判定
- 批改结果进入错题分析流程
### 3.4 习惯追踪模块
- 长期记忆存储结构化数据，例如：
  ```json
  {
    "user_id": "u_123",
    "type": "habit",
    "content": "几何证明题经常忘记添加辅助线，导致思路中断",
    "source": "quiz_session_20260907",
    "timestamp": "2026-09-07T22:00:00"
  }
  ```
- 查询习惯时按 user_id 过滤检索，LLM 汇总生成习惯优缺点报告
---
## 四、优化策略实现方案
### 4.1 知识库：动态切片 + 与记忆隔离
**不能固定切片大小 → 语义分块策略：**
| 内容类型 | 分块策略 |
|----------|----------|
| 题目 | 以"题干-选项-答案-解析"为完整 Chunk |
| 教材 | 按"定义-定理-例题"结构分块 |
| 错题报告 | 单份报告为一个 Chunk |
推荐使用**语义相似度分块**：计算相邻句子 Embedding 相似度，低于阈值处切分。
**数据库隔离（双 Collection）：**
- `knowledge_base`：题库、数学概念，全局共享，仅检索
- `user_long_term_memory`：用户错题习惯、历史报告，**检索时必须带 user_id 过滤**
### 4.2 短期记忆 vs 长期记忆
| 维度 | 短期记忆 | 长期记忆 |
|------|----------|----------|
| 载体 | State.messages / Redis 会话 | 向量数据库 Collection |
| 内容 | 当前对话、正在刷的题、刚上传的文件 | 错题共性、做题习惯、历史报告 |
| 生命周期 | 当前会话/刷题组 | 永久（可更新） |
| 写入时机 | 实时 | 完成错题报告后由 Memory Manager 提取 |
### 4.3 ReAct 循环控制
```python
# 条件边伪代码
def should_continue(state):
    # 条件1：最大循环次数
    if state["react_loop_count"] >= 8:
        return "force_output"
    # 条件2：信息增益判定
    current_vec = embed(state["latest_thought"])
    if state["last_result_vector"] is not None:
        sim = cosine_similarity(current_vec, state["last_result_vector"])
        if sim >= 0.8:  # 语义相似度达 80%，思考停滞
            return "force_output"
    return "continue_react"
```
### 4.4 工具调用防重防错
1. **结构化工具定义**：Pydantic 严格校验参数（板块为枚举值 `['函数', '方程', '几何']`）
2. **防重拦截**：记录 `tool_name + arguments_hash`，命中则拒绝调用并回注提示：
   > "该工具已用相同参数调用过，请使用已有结果或更改参数"
3. **依赖链约束**：OCR → 批改 的调用顺序在 Prompt 与代码层双重约束
4. **Fallback**：工具报错后错误信息重新注入，要求 LLM 自我纠正
---
## 五、System Prompt 设计与解析
```markdown
# Role
你是一个专业的初中数学辅导 Agent。你的职责是帮助用户刷题、批改作业、
分析错题并追踪学习习惯。
# Core Directives (核心指令)
## 1. 记忆处理机制
- **短期记忆：** 关注当前对话上下文、用户正在进行的刷题组别、
  刚上传的PDF/图片内容。不要对一道题的解答生成冗长的"习惯报告"。
- **长期记忆：** 只有在用户完成一组完整的刷题（或上传成绩单批改完毕）后，
  你才需要提炼用户的错题共性、做题习惯（如计算粗心、定理混淆），
  并调用 `update_long_term_memory` 工具进行存储。
## 2. ReAct 循环约束
- 思考必须遵循 Thought -> Action -> Observation 流程。
- **限制1：** 最大思考循环次数为 8 次。超过 8 次请直接基于现有信息
  给出最优回答。
- **限制2：** 信息增益判定。若当前轮 Thought 与上一轮语义相似度达到 80%
  （陷入重复尝试或思维停滞），立即停止循环，输出当前结论。
## 3. 工具调用规范
- 严禁使用相同参数重复调用同一工具。已调用过
  `fetch_quiz(topic="函数", type="选择题")`，绝不能再次调用。
- 调用工具前确保参数完整且符合格式。例如：解析PDF必须先调用OCR
  获取文本，再调用批改工具。
## 4. 错题分析策略
- **不要做即时反馈：** 用户做错一道题时，仅提示"回答错误，请继续
  下一题或选择查看解析"。
- **阶段性报告：** 用户输入"交卷"、"结束刷题"或完成成绩单批改时，
  调用 `generate_error_report` 工具汇总本次所有错题，输出结构化的
  错题报告和修改建议。
# Workflow
1. 识别用户意图（刷题、批改、习惯查询、闲聊）。
2. 若为刷题，通过 `fetch_quiz` 获取题目，等待用户作答。
3. 若为批改，调用 `ocr_document` 和 `grade_assignment`。
4. 任务完成后，触发记忆与报告生成。
```
**优化点解析：**
1. 明确界定短期/长期记忆触发时机 → 防止记忆混淆、乱写库
2. 硬性规定"不做即时反馈" → 解决"刷一题给一次报告"问题
3. Prompt 中告知 ReAct 约束 → 与代码层条件边形成双保险，加速收敛
4. 自然语言约束工具防重 → 配合 hash 拦截，双重防护
---
## 六、从 0 到 1 落地步骤
### 🗺️ 总体路线（建议 6 个阶段，约 8-12 周）
```
Phase 0 ─► Phase 1 ─► Phase 2 ─► Phase 3 ─► Phase 4 ─► Phase 5
环境搭建   基础数据   最小闭环   核心优化   增值功能   上线运维
(1周)     (1-2周)    (2-3周)    (2周)     (1-2周)    (持续)
```
---
### Phase 0：环境与基础设施搭建（约 1 周）
**目标：跑通最小技术链路**
- [ ] 确定 LLM 与 API（GLM 系列），验证工具调用能力
- [ ] 部署向量数据库（推荐先用 Qdrant Docker 单机版）
- [ ] 部署 MySQL（题库/用户/答题记录）+ Redis（会话缓存）
- [ ] 初始化 LangGraph 项目骨架，跑通官方 Hello World
- [ ] 确定 Embedding 模型并验证中文效果（推荐 BGE 系列中文模型）
**验收标准：** 一条消息能走完 LLM → State → 输出的完整回路。
---
### Phase 1：数据与工具层建设（约 1-2 周）
**目标：Agent 的"弹药库"就绪**
**1. 题库建设**
- [ ] 收集/整理初中数学题目（人教版/北师大版），覆盖函数、二元一次方程、几何图像
- [ ] 数据结构设计：
  ```json
  {
    "question_id": "Q001",
    "topic": "函数",
    "type": "选择题",          // 选择题/填空题/应用题
    "difficulty": 3,           // 1-5
    "stem": "题干...",
    "options": ["A...", "B...", "C...", "D..."],
    "answer": "B",
    "analysis": "解析...",
    "knowledge_points": ["一次函数图像", "斜率"]
  }
  ```
- [ ] 初期目标：每个板块至少 200 题，共 600+ 题
**2. 工具开发（Tool Layer）**
- [ ] `fetch_quiz(topic, type, count, difficulty)` — 拉题工具
- [ ] `check_answer(question_id, user_answer)` — 客观题判分（代码判定，不走 LLM）
- [ ] `grade_application_question(question, user_answer)` — 应用题/过程判定（LLM + 规则）
- [ ] `ocr_document(file)` — 图片/PDF 解析（对接 PaddleOCR 或多模态 LLM）
- [ ] `calculator(expression)` — 数学表达式计算（SymPy）
- [ ] 全部工具用 Pydantic 定义参数 Schema
**验收标准：** 每个工具可独立通过单元测试调用。
---
### Phase 2：最小可用闭环（约 2-3 周）⭐ 核心
**目标：用户能完整走通"选板块 → 刷题 → 交卷 → 错题报告"**
**1. 搭建 LangGraph 主干**
- [ ] 实现 `MathAgentState` 状态定义
- [ ] 实现 **Intent Router** 节点（先用 LLM 分类 + 关键词兜底）
- [ ] 实现 **Quiz Manager** 节点：管理 quiz_session 的出题、收答、判分
- [ ] 实现 **Error Analyzer** 节点：交卷后汇总错题生成报告
**2. 编写 System Prompt v1**
- [ ] 按第五节模板编写，重点保证"非即时反馈"逻辑生效
- [ ] 建立 Prompt 版本管理（建议 prompts/ 目录 + 版本号）
**3. 刷题流程联调**
```
用户: "我要刷函数的选择题"
  → Intent Router → fetch_quiz → 返回第1题
用户: "选B"
  → check_answer → "回答错误/正确"（不分析）
...（循环）
用户: "交卷"
  → Error Analyzer → 错题报告
```
**验收标准：** 内部测试人员可完整跑通一次刷题 + 报告，且单题错误时 Agent 不会输出分析报告。
---
### Phase 3：四项核心优化落地（约 2 周）
**目标：满足所有硬性优化要求**
**1. 向量知识库（动态切片 + 隔离）**
- [ ] 实现语义分块器（相似度阈值切分，替代固定 chunk_size）
- [ ] 建立两个 Collection：`knowledge_base` 与 `user_long_term_memory`
- [ ] 检索工具 `search_knowledge(query)` 与 `search_user_memory(query, user_id)` 分离
- [ ] 验证：不同用户检索记忆互不串扰（用两个测试 user_id 交叉验证）
**2. 记忆分层管理**
- [ ] **Memory Manager** 节点开发：
  - 触发时机：Error Analyzer 完成后自动触发
  - 用 LLM 从错题报告提取结构化习惯数据 → Embedding → 写入长期记忆
- [ ] 短期记忆：quiz_session 存 Redis（TTL 设 24h），State 只存引用
**3. ReAct 循环控制**
- [ ] 实现 `react_loop_count` 计数，条件边强制 8 次上限
- [ ] 实现信息增益：每轮 Thought Embedding 后与 `last_result_vector` 比对，相似度 ≥ 0.8 即终止
- [ ] 构造测试用例：故意给 LLM 一个无解任务，验证两种终止机制都能触发
**4. 工具调用防重防错**
- [ ] 实现 `tool_call_history` + `arguments_hash` 拦截器
- [ ] 命中重复调用时，向 LLM 回注提示让其自我纠正
- [ ] 工具错误 Fallback 测试（如传入不存在的 topic）
**验收标准：** 四项优化各有自动化测试用例，全部通过。
---
### Phase 4：增值功能（约 1-2 周）
**目标：补齐批改与习惯功能**
- [ ] **批改流程**：OCR → 结构化 → 判分 → 接入 Error Analyzer
  - 重点测试：手写体识别准确率、PDF 多页处理
- [ ] **习惯查询**：`search_user_memory` + LLM 汇总，输出习惯优缺点报告
- [ ] **成绩单上传**：复用批改流程，解析成绩单格式
- [ ] **Prompt 迭代**：基于 Phase 2-3 的 badcase 记录，优化 System Prompt v2
- [ ] 建立基础评测集（20-30 个典型对话脚本），每次改动回归测试
**验收标准：** 四大功能（刷题/错题/批改/习惯）全部可用。
---
### Phase 5：上线与运维（持续）
- [ ] **接口封装**：FastAPI 包装 LangGraph，暴露 SSE/WebSocket 流式接口
- [ ] **前端对接**：刷题界面、报告展示页、文件上传
- [ ] **可观测性**：
  - 日志：每次 ReAct 循环、工具调用、记忆写入全链路 trace（可用 LangSmith）
  - 监控：循环终止原因分布（达到上限/增益触发）、工具调用错误率
- [ ] **灰度上线**：先 50 名内测用户，收集 badcase
- [ ] **数据飞轮**：badcase → 优化 Prompt / 增补题库 → 回归评测
- [ ] **安全合规**：未成年人数据保护、内容审核（题目/报告输出过滤）
---
### 📌 关键里程碑速查表
| 阶段 | 交付物 | 判断"可以进入下一阶段"的标准 |
|------|--------|------------------------------|
| Phase 0 | 技术链路跑通 | 一条消息完整回路 OK |
| Phase 1 | 题库 + 工具集 | 工具单测全过 |
| Phase 2 | 刷题最小闭环 | 完整刷题+报告可跑通，无即时反馈 bug |
| Phase 3 | 四项优化 | 优化项自动化测试全过 |
| Phase 4 | 全功能 | 评测集通过率 > 85% |
| Phase 5 | 上线 | 灰度期无 P0 级问题 |
---
## 七、风险与应对
| 风险 | 等级 | 应对方案 |
|------|------|----------|
| OCR 手写识别准确率低 | 高 | 多模态 LLM 兜底；OCR 置信度低时要求用户确认 |
| LLM 意图路由误判 | 中 | 关键词规则兜底；提供前端按钮直接路由（绕过自然语言） |
| 长期记忆越权串扰 | 高 | 代码层强制 user_id 过滤，定期做隔离审计测试 |
| 刷题报告触发不准（逐题出报告） | 中 | 代码层硬约束：Error Analyzer 只由"交卷"事件触发，不依赖 LLM 自觉 |
| ReAct 循环空转烧 token | 中 | 双重终止机制 + 监控循环次数分布 |
---
## 附录：技术栈依赖清单
```
langgraph >= 0.2
langchain >= 0.3
qdrant-client / pymilvus
sqlalchemy + pymysql
redis
pydantic >= 2.0
paddleocr（或 GLM-4V 多模态）
sympy（数学计算）
fastapi + uvicorn（服务化）
langsmith（可观测性，可选）
```
💡 落地建议补充说明
优先级排序：Phase 2 的最小闭环是整个项目的生命线，建议先跑通“函数选择题”一个板块，再横向扩展，切勿一开始就追求全功能。
Prompt 与代码双保险原则：你的 4 项优化要求，凡是关键行为（如“不逐题出报告”、“记忆隔离”），都不要只依赖 Prompt，务必在 LangGraph 代码层用条件边/拦截器硬约束——LLM 的自觉性不可靠。
评测先行：从 Phase 2 就开始积累 badcase 评测集，这是后期迭代 Prompt 时唯一可信的依据。
如需我输出某个 Phase 的具体代码（如 Phase 3 的信息增益判定器、Phase 2 的 LangGraph 图构建代码），随时告诉我！