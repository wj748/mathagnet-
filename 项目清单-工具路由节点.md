# 项目清单：工具 · 路由 · 节点 · 状态 · 接口 · 配置 · 验收

> 更新：2026-09-09 ｜ 与代码同步（以 src/math_agent 实际实现为准）

---

## 一、工具清单（tools/，registry 统一注册，共 11 个）

| # | 工具名 | 用途 | 依赖前置 | 文件 |
|---|---|---|---|---|
| 1 | `fetch_quiz` | 按板块/题型/难度拉题，返回题目列表 | — | quiz.py |
| 2 | `check_answer` | 客观题判分（选择题先映射选项下标再比较） | — | quiz.py |
| 3 | `ocr_document` | 图片/PDF/文本 → 纯文本（白名单 data/uploads/ + 扩展名） | — | ocr.py |
| 4 | `grade_assignment` | 整卷批改：OCR 文本逐题切分判分 | ⚠ 必须先 `ocr_document` | grading.py |
| 5 | `grade_application_question` | 应用题按过程给分（知识库比对 → LLM 兜底） | — | grading.py |
| 6 | `calculator` | SymPy 精确计算/解方程/求导，可验算 | — | calc.py |
| 7 | `search_knowledge` | 全局题库/概念/定理检索（Qdrant knowledge_base） | — | memory_tools.py |
| 8 | `search_user_memory` | 用户长期记忆检索（⚠ 强制 user_id，缺失抛 IsolationError） | — | memory_tools.py |
| 9 | `update_long_term_memory` | 写入习惯/薄弱点（仅完整刷题或批改后，单题禁用） | — | memory_tools.py |
| 10 | `generate_error_report` | 汇总错题出结构化报告（错因占比/逐题分析/建议） | — | report.py |
| 11 | `explain_knowledge_point` | 知识点讲解（定义/要点/易错点/口诀 + 配套例题） | — | teaching.py |

**工具调度约束**（tools/registry.py，代码层硬约束）：
- 防重：`tool_name + args_hash` 命中即拒绝，回注自我纠正提示；
- `tool_call_history` 每次调用都记录（跨轮由 compressor 压缩瘦身）；
- OCR 后端降级链：paddleocr → PDF 文本层 → 多模态 LLM → 纯文本。

---

## 二、意图路由清单（graph/router.py）

**意图集合**：`quiz / submit / grade / habit / learn / chat`（路由规则优先，LLM 兜底）

**规则优先级**（`_rule_route(text, in_quiz, mode, in_learn)`，从上到下首个命中即返回）：

| 序 | 条件 | 意图 | 置信度 | 说明 |
|---|---|---|---|---|
| 1 | 空输入 | chat | 0.3 | |
| 2 | **学习模式内**（`mode=learn` 或已有 `learning_session`）且命中概念咨询 | chat | 0.9 | 学习中提问走答疑，不打断学习 |
| 3 | **学习模式内**且非刷题/批改/习惯/交卷指令 | learn | 0.9 | 学习模式内默认继续学习 |
| 4 | 概念咨询：什么是/怎么求/为什么/讲解/我不懂/举个例子… | chat | 0.9 | ⚠ 不能加"解析/提示"（会挡刷题单题反馈） |
| 5 | 刷题中 + 短输入（A-D 选项 / 纯数字/分数） | quiz | 0.95 | 作答判定 |
| 6 | 交卷：交卷/结束刷题/生成报告/提交… | submit | 0.95 | |
| 7 | 批改：批改/改作业/试卷/判一下分… | grade | 0.85 | 含 `判\s*(一\s*下|下)?\s*分` 口语 |
| 8 | 习惯：习惯/薄弱/学情/进步… | habit | 0.85 | |
| 9 | 学习：我要学/教我/开课/知识点讲解/系统学习… | learn | 0.9 | ⚠ 在 `quiz` 之前，避免"我想学函数"被当刷题 |
| 10 | 刷题：刷题/出题/来几道/专项/测验… | quiz | 0.9 | |
| 11 | 刷题会话未结束的其它输入 | quiz | 0.7 | 默认继续作答 |
| 12 | 以上全不中 | （空） | 0 | 交 LLM 分类 / 默认闲聊 |

**学习模式相关正则**（teaching.py / router.py）：`_P_LEARN`（我要学/教我/开课/…）、
`_P_EXIT`（退出学习/不学了/结束学习）、`_P_NEXT`（继续/下一个/下一节）、
`_P_AGAIN`（没听懂/再说一遍/重讲）、`_P_EXAMPLE`（换个例题/再举个例子）。

---

## 三、LangGraph 节点与流转清单（graph/builder.py）

**节点（9 个）**：`intent_router` → 分流至：

| 意图 | 节点 | 后续 |
|---|---|---|
| quiz | `quiz_manager` | → finalize（单题反馈/发题；交卷转 error_analyzer） |
| submit | `error_analyzer` | 有报告 → memory_manager → finalize；无 → finalize |
| grade | `grading` | 有 grade_result → error_analyzer → …；无 → finalize |
| habit | `habit` | → finalize |
| **learn** | **`knowledge_tutor`** | → finalize（阶段机：catalog→teach→quiz→graded→next） |
| chat | `react` | 自循环直到终止条件 → finalize |

**ReAct 终止硬约束**（react.py）：`react_loop_count ≥ 8`（max_loop）或信息增益 ≥ 0.80（information_gain）或 Action 判 finish → `loop_stop_reason` 记录，`/api/metrics` 可查分布。

---

## 四、State 字段清单（state.py，MathAgentState）

| 分组 | 字段 | 用途 |
|---|---|---|
| 对话 | `messages / user_input / user_id` | 输入与身份 |
| 意图 | `intent / intent_confidence` | 路由结果 |
| 刷题 | `quiz_session{questions,index,records,finished…} / pending_answer / submit_event / awaiting_user` | 会话；`submit_event` 是交卷硬约束标记 |
| 批改 | `ocr_payload / grade_result` | OCR 输出与整卷判分 |
| 记忆 | `short_term_memory / long_term_memory_hits / memory_written` | 观察窗与记忆命中 |
| 报告 | `error_report / final_report / _records` | 错题报告链路 |
| ReAct | `react_loop_count / react_thoughts / last_result_vector / loop_stop_reason` | 循环控制与信息增益 |
| 工具 | `tool_call_history / tool_blocked_notice` | 防重历史与拦截提示 |
| 输出 | `response / trace` | 回复与执行轨迹 |
| 压缩（动态注入） | `conversation_digest` | 每轮由 compressor 生成，注入 ReAct 三 prompt |

> 跨轮持久化键：`tool_call_history / short_term_memory / quiz_session`——每轮从 last_state 取出→裁剪→**显式回注** payload。

---

## 五、API 接口清单（api/server.py，127.0.0.1:8787）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/session` | — | user_id 换 HMAC token（30 天有效） |
| POST | `/api/chat` | token | 同步对话（可选 `mode=quiz\|learn`） |
| GET | `/api/chat/stream` | token | SSE：trace → delta 逐字 → done（同支持 `mode`） |
| GET | `/api/curriculum` | — | 课程目录：板块 → 知识点列表（供学习模式选择器） |
| GET | `/api/traces` | — | ① 调用链：最近 N 轮明细 + 聚合统计（意图/终止/工具/耗时） |
| GET | `/trace` | — | ① trace 面板页面（自动刷新、按意图/用户过滤） |
| POST | `/api/upload` | token | 上传作业（净化+10MB 上限）→ OCR 批改 |
| GET | `/api/stats` | token | 题库统计 + 本人做题数/正确率 |
| GET | `/api/metrics` | — | 循环终止/工具错误率/意图分布/压缩量 |
| GET | `/api/health` | — | 轻量健康检查（不触发 BGE 加载） |

---

## 六、关键配置清单（config.yaml）

| 段 | 键 | 默认 | 说明 |
|---|---|---|---|
| graph | max_react_loops / info_gain_threshold | 8 / 0.80 | ReAct 双重终止 |
| memory | context_budget / recent_turns / max_summary_lines / max_entities | 1600 / 6 / 12 / 10 | 压缩预算与窗口 |
| memory | max_turns_kept / obs_window | 60 / 8 | 原始轮次与观察窗上限 |
| memory | hist_full / hist_compact_beyond | 8 / 24 | 工具历史完整保留/压缩阈值 |
| security | require_token | false | ⚠ 公网必须 true |
| security | cors_origins / max_upload_mb | 本机 8787 / 10 | CORS 白名单 / 上传上限 |
| security | agent_ttl / agent_max | 1800 / 100 | Agent 池淘汰 |
| llm | provider auto + GLM_* 环境变量 | — | 无 Key 自动离线规则模式 |
| embedding | BAAI/bge-small-zh-v1.5，无 torch 降级 hashing | — | |
| vectorstore | qdrant 本地 data/qdrant，双 Collection | — | |
| database | sqlite data/math_agent.db（已开 WAL/busy_timeout=30s） | — | |

---

## 七、验收清单（scripts/，七套）

| 脚本 | 规模 | 覆盖 |
|---|---|---|
| eval_optimizations.py | 34/34 | 四项优化 + 判分准确性回归（选择题零误判） |
| eval_conversation.py | 11/11 | 34 条意图路由 + 10 条学习模式路由 + 行为约束 |
| eval_memory.py | 21/21 | 多轮压缩/释放、state 有界、实体保留、压缩后防重仍拦截 |
| eval_security.py | 16/16 | OCR 白名单、鉴权、上传净化、并发锁、WAL、session_id |
| eval_teaching.py | 19/19 | 课程大纲完整性、离线讲解、学习闭环、硬约束、讲解工具 |
| eval_badcase.py | 42/42 | 8 组历史踩坑：判分/LaTeX/模板/路由/隔离/安全/审核/Redis 降级 |
| pytest tests/ | 7 passed | 冒烟 |

端到端演示：`python scripts/run_cli.py --demo`

---

## 八、硬约束清单（全部代码层强制，不依赖 LLM 自觉）

1. 不逐题出报告——Error Analyzer 只由 `submit_event` 或批改完成触发；
2. 记忆隔离——`search_user_memory` 缺 user_id 抛 `IsolationError`；
3. 循环终止——≥8 轮或信息增益 ≥0.80 强制 finalize；
4. 工具防重——`name+args_hash` 命中即拦截；
5. 选择题判分——先映射选项下标再比较，选项前缀严格正则；
6. OCR 白名单——仅 `data/uploads/` 内白名单扩展名；
7. 身份鉴权——token 为准，自报 user_id 不采信（require_token=true 时无 token 401）；
8. 资源有界——state 裁剪回注、对话压缩预算、Agent 池 TTL/容量、上传大小上限；
9. 结构化输出不被 LLM 覆写——报告/讲解骨架由代码层渲染，LLM 只追加建议段；
10. 未成年人保护——输入审核拦截（不进主流程）、PII 脱敏（日志/trace/记忆前打码）、
    输出侧拦截索取隐私话术（safety.py，纯规则离线生效）。
