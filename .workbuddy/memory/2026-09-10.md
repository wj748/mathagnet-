# 2026-09-10

## 全面审查（问题/bug/不足/优化方向）
- 产出 `隐患审查报告-20260910.md`：3 P0 + 6 P1 + 13 P2 + 仓库卫生；关键项均已人工核实。
- P0：① _P_QUIZ/parse_quiz_request 缺概率/统计/三角函数 → 刷题失效或误板块；
  ② 学习模式切刷题后作答被 knowledge_tutor 吞（session_mode 未清）；
  ③ ocr_payload/grade_result 不在 carried → OCR 二次确认死循环。
- 已当场修复 3 笔误：teaching.py 判别式 b²-4c→b²-4ac；web 符号栏 x_n 插入 _1→_n；
  .gitignore 补 .secret_key/*.log/.idea//test*.json。
- 审查确认无问题：前端 XSS 防护、上传路径穿越、WAL、learning_session 跨轮、
  38 知识点数学内容（除判别式外）。
- 教训：**新增板块时必须同步检查 router._P_QUIZ / nodes.parse_quiz_request /
  eval_conversation 用例**——CURRICULUM 扩容漏掉了刷题侧路由，评测也没覆盖。

## 按审查报告实施修复（当天全部完成）
- P0×3 全修：router _P_QUIZ+nodes 板块映射（新板块回退综合+提示）；learn 分支加
  `and not in_quiz`；carried 加 ocr_payload/grade_result。
- P1：判分 `_strip_unit`（单位表注意 `平方?` 写法坑——? 只作用于"方"，"千米"曾全失配，
  改 `(?:平方|立方)?`）+ `_multi_equal`（或=任一、、=答全，用户侧统一 _ALT_USER 拆分）；
  safety `_normalize_for_match`（**不要 lower()**——词表含大写 QQ，小写化反而失配）；
  OCR 文本出工具前 mask_pii。
- P2：出题去重 exclude_ids+拉空放开重试；情绪表达不判分；上传 finally unlink；
  异常收敛进 logging；监控接口 _require_admin（require_token=true 时生效）。
- 更正：file.read(MAX+1) 实际有界（SpooledTemporaryFile），原 P1-3 内存 DoS 判定过重。
- 全量回归绿：38/38+13/13+11/11+34/34+21/21+16/16+42/42+19/19+pytest 7。
  服务重启，端到端"刷概率题"验证通过（回退提示+出题正常）。

## 双视角不足清单（工程师 + 用户）
- 产出 `双视角不足清单-20260910.md`：工程师 20 条 / 用户 19 条，均已回源码核对行号。
- **最被低估的坑**：`scripts/eval_optimizations.py:32` `check()` 只 print 不 assert →
  被 pytest 收集后失败也判 PASS，**"七套全绿"在 CI 语境下是假绿**。改法：check 内 raise
  AssertionError 或 test 末尾 assert 汇总。这是下次接 CI 前必须修的。
- 用户视角三条致命：① `web/index.html:251` USER=Math.random() 刷新丢身份+进度；
  ② `nodes.py:159` 答错只回"❌回答错误"直接跳题，解析要主动打"解析"二字，
  且 analysis 空时给"（该题暂无解析）"；③ `index.html:473` 上传不检查 r.ok，
  HEIC（iPhone 默认）不在 ocr.py:30 白名单 → 失败也显示"已上传"。
- 工程师三条根因：nodes.py 三处 `except: pass`（154/201/278，答题记录静默丢）；
  InMemorySaver+内存 metrics 重启失忆；板块命名三套并存（CURRICULUM 6 /
  TOPIC_ENUM 4 / _NO_BANK_TOPICS 映射）无单一真源。
- 交汇：状态不持久 / 静默失败 / 教学深度不足 —— 修一个同时消两边症状，最优先。
- 其余：伪流式 SSE、LLM 无重试、find_point_index 子串误命中（"分式"⊂"分式方程"）、
  无 Dockerfile/CI、requirements 全 >= 未 pin、README:251 板块描述已漂移、
  search_knowledge 未用 score_threshold、无迁移脚本、无激励/家长视图/隐私说明。

## 双视角清单修复实施（2026-09-10 下午，续）
- **#29-#31（前次会话完成）**：eval 真断言门禁（PYTEST_CURRENT_TEST 触发 raise）、
  三处 except:pass → log.exception+metrics、分层提示 _hint_for、会话/轨迹 SQLite 落盘。
- **#32 前端收尾**：上传逻辑抽 doUpload()，FAB/选完自动上传/上传后清空 file input；
  新增 POST /api/reset（repository.reset_user_data 清 attempts/sessions/states +
  内存 Agent 弹池 reset）+ 前端「重置我的学习数据」confirm 流程。
- **#33 架构统一**：新建 `src/math_agent/topics.py` 单一真源（BANK_TOPICS/
  TEACHING_TOPICS/TEACHING_TO_BANK/to_bank_topic）。关键发现：teaching._EXAMPLE_TOPIC
  与 nodes._NO_BANK_TOPICS 对"锐角三角函数"映射矛盾（几何图像 vs 综合），实测题库
  三角函数题分布 10/4/9（函数/几何/方程，均噪声）→ 统一映射综合。find_point_index
  改两遍最长匹配（防"分式"⊂"分式方程"劫持）；search_knowledge 内置
  retrieval.score_threshold（默认 0.10，与 memory_tools/grading 共用 config）；
  quiz.py MAX_COUNT=20 与 nodes.py parse 对齐（原 le=50 不一致）。
  eval_badcase 新增 43-46 板块真源回归 → 46/46。
- **#34 工程化**：requirements.lock.txt（pip freeze 108 项）、Dockerfile
  （python:3.12-slim + torch CPU 索引 + HEALTHCHECK + HOST/PORT env，
  server.main 支持 HOST/PORT 环境变量）、.github/workflows/ci.yml
  （pytest + eval_memory/eval_teaching；判分/badcase 依赖本地题库留本地跑）、
  docker-compose 加 app 服务（profile full，默认行为不变）。
  注意：.env.example 早已存在（清单写"无"过时）。
- **#35 全量回归**：34/34、11/11、21/21、19/19、46/46、pytest 7 passed。
  **eval_security 曾 15/16**：非代码回归，是 eval 自身一条永假断言
  （`UPLOAD_DIR in Path(api返回的纯文件名).parents`——API 返回净化文件名且产物即删，
  该断言从不成立；此前"16/16"是假绿）。已修正断言语义（safe_join 不逃逸）→ 16/16。
- 服务已重启（task Qw28KQ），E2E 通过：health / session / SSE 刷题 / stats /
  reset（cleared sessions=1；无 token 走兼容路径 200 与 require_token=false 设计一致）。
  GET / 已含 fab-upload/btn-reset/doUpload 新代码。
- **清单 1-12/14 全部落地**；遗留 13（SSE 真流式+LLM 重试）、15（家长视图）、
  16（无障碍增强）需更大改动未列入本轮。实施记录见清单第六节。
- **清理无用文件**：用户确认后删除 A 组测试残留 4 项（test.json/test2.json/
  vision_test.png/questions.sqlite 0字节无引用）。沙箱禁回收站 API（Add-Type/COM 被拦截），
  改为备份至 data/cleanup_backup_20260910/ 再删除（.gitignore 已加 cleanup_backup_*）。
  B 组 data/*.log（11 个，约40KB，可再生）经询问用户选择保留。
