- **多用户并发治理落地（concurrency.py 新模块）**：①UserGate 同用户互斥（非阻塞
  抢占，第二条 429）+1s 滑窗 QPS（per_user_qps=3，到达即计数）；②GlobalBreaker
  全局信号量 max_agent_tasks=8 保护下游 LLM 配额 + max_inflight=32 熔断 +
  queue_wait_s=15 排队超时降级 503；③IdempotencyStore 请求 ID 幂等（TTL 300s+LRU
  500，key=user:rid，trace 不入缓存，重放带 idempotent_replay=true）；④InMemoryStore
  全操作 RLock、compressor 会话版本戳（version 单调递增）；⑤upload 的 agent.invoke
  移入 anyio.to_thread（原来直接阻塞事件循环）。chat/chat_stream/upload 全接入
  _admit()；SSE 的准入在响应头前完成（429 状态码可达），释放在 gen finally。
  E2E：同用户并发 [200,429]✓、rid-1 重放 idempotent_replay=true✓、metrics
  concurrency{admitted:4,busy:1,replay:2}/breaker 段✓。pytest 18 passed+6 skipped、
  eval_memory 21/21、eval_conversation 11/11。
- **eval_security 假失败一次（15/16）**：非并发改动回归——.env 的
  MATH_AGENT_SECURITY__REQUIRE_TOKEN=true 泄漏进 eval，第 2 节假定 false 起跑未显式
 设定。已修：第 2 节入口显式 cfg.raw 置 false → 16/16。教训：eval 断言必须显式
  设定自己依赖的前置状态，不能依赖 .env 当前值。
- 技术文档新增 §9.5 并发治理；测试文件 tests/test_concurrency.py（8 用例）。
  边界：锁均为进程内，多实例部署需 Redis 分布式锁（docstring 已声明）。
- **工具链失败治理落地（toolguard.py 新模块，规格：失败分类/持久化/重规划/降级）**：
  ①classify_failure 三分类（fatal 优先于 transient 判定）；②execute_with_guard：
  线程池超时（ocr 90s 特例）+ 指数退避重试（仅 transient、仅当前步骤，写类工具
  retryable=False 保幂等——update_long_term_memory 已标记）+ 每工具熔断（滑窗
  5 次失败率 0.8 打开、冷却 30s 半开，快速拒绝带 blocked 标记）；③sanitize_error
  脱敏（路径/token/邮箱）后才能进 ToolResult/trace/prompt；④StepLedger 台账：
  task_id/step_id/tool/args_hash/status 按步落 Redis（TTL 1h，completed_map 供续跑）；
  ⑤react 节点：失败（含 blocked 同参重复！）→ replan 计数（上限 2）→ 超限降级
  出部分结果（loop_stop_reason: fatal_error/degraded）；fallback_tools 强制换用
  备选工具；trace 新增 react[n]tool=xxx:ok/blocked/fail。
- **E2E 实证**：真实对话 trace ['search:ok','calculator:ok','calculator:blocked'×3]→
  replan:2 计数后链路终止（此前 blocked 被跳过导致 3 轮空转，已修）；
  台账 35 键落 Redis。回归：pytest 26 passed+6 skipped、eval_memory 21/21、
  eval_badcase 49/49（新增 47-49 toolguard 回归）、eval_security 16/16、
  eval_conversation 11/11。
- **教训×3**：①同文件多处 Edit 并行批量提交三次静默丢编辑（metrics 三处、
  toolguard 返回值、react cls 行）——必须逐个改+逐个 grep 验证；②eval 前置状态
  必须显式设定（33/34 因 .env 启用 Redis 而假失败，已修 eval）；③
  RedisStore 懒连接构造不报错≠可达——补 ping 探活（2s 超时），否则坏地址静默
  假装 Redis 正常；redis-cli --scan 在管道下输出不可靠，用 keys 交叉验证。
