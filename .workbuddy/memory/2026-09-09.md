
## 09:10 项目清单
- 产出《项目清单-工具路由节点.md》：10 个注册工具（含依赖前置）、路由规则 9 级优先级表、
  8 节点流转、State 全字段、7 个 API、config 关键键、五套验收、8 条硬约束。
- 备注：注册工具实为 10 个（calculator/fetch_quiz/check_answer/grade_assignment/
  grade_application_question/ocr_document/search_knowledge/search_user_memory/
  update_long_term_memory/generate_error_report），此前文档写"9 个"系口径误差。

## 12:00 接入智谱 GLM API Key + 修复「LLM 覆写结构化输出」隐患
- 用户给出智谱 GLM Key，已写入 `.env`（GLM_API_KEY / GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
  / GLM_MODEL=glm-4-flash）。config.py 内置 `_load_dotenv()` 免依赖；`.env` 已在 .gitignore。
- **接 Key 后立刻暴露 3 处同类隐患（离线模式完全看不出，接 Key 后必须重跑全量验收）**：
  1. `habit_node` 整份报告交 LLM 改写 → 标题「🧭 学习习惯报告」变「🌟 学习小记」（eval 34→33）
  2. `analysis.llm_polish_report` 错题报告整段交 LLM → 正确率/错因数字有改写（幻觉）风险
  3. `teaching.llm_lecture` 讲解整段交 LLM → 「◆ 定义/要点/易错点/口诀」骨架丢失（19→18）
- **统一修法（新增硬约束 7）**：结构化正文由代码层渲染，LLM 只**追加**补充段、永不覆写：
  - habit → `body + "\n\n💡 老师建议\n" + llm_text`（prompt 改为只补 2-3 条建议、120 字内、不重复数字）
  - `llm_polish_report` → 同上
  - teaching → `rule_lecture(kp, topic, extra=llm_text)`，LLM 段落在「◆ 老师讲讲」；
    `build_lecture` 的 source 由「LLM 是否可用」改为「是否真拿到 LLM 文本」
- 全量回归（**LLM 在线模式**）：34/34 + 11/11 + 21/21 + 16/16 + 19/19 + pytest 7 passed
- 文档同步：README 新增第 7 节「知识点讲解 Agent」+ 硬约束 7/8 + 六套验收表 + security 配置行；
  《项目清单》改 11 工具 / 路由 12 级 / 9 节点 / 加 `/api/curriculum` / 六套验收；
  《项目全流程梳理》加 learn 分支 + 11 工具 + 释放意图列表补 learn
- **运维坑**：Qdrant 本地模式只允许单进程——跑评测前必须先停 API 服务，否则报
  `RuntimeError: Storage folder data/qdrant is already accessed by another instance`，
  表现为「隔离类用例 + pytest 集体失败」，极易误判为代码回归。
  Git Bash 下 `taskkill //PID` 不可用（无效参数），改用 PowerShell 工具 `Stop-Process -Id <pid> -Force`。

## 14:00 Phase 5 五项待办全部完成（README 第 8 节）
1. **自研 trace 面板**：新建 `trace.py`（TurnTrace 环形缓冲 500 轮，record_turn 全量 try/except
   绝不影响主流程；输入/回复经 mask_pii 脱敏截断）+ `GET /api/traces`（明细+stats：
   轮次/错误率/P50/P95/意图/终止/工具）+ `/trace` 页面（web/trace.html，自动刷新、过滤）。
   **顺手修复 metrics.py 静默 bug**：`record_conversation`/`_CONV` 曾缺失，compressor 的
   try/except 吞掉 AttributeError → conversation 指标一直为 0；reset() 也曾 NameError。
2. **GLM-4V 视觉识别**：`llm.get_vision_client()`（GLM_VISION_MODEL，默认 glm-4v-flash，
   **独立于文本 client**——glm-4-flash 不支持图片）；OCR 多模态兜底 3 次重试（1210 偶发）。
   **关键坑**：data URL 必须 `data:image/png;base64,...`，原代码写成 `data:png` → 1210 图片
   解析错误且 100% 复现；扫描件 PDF 无文本层 → 明确提示转图片，不再误调视觉模型。
3. **badcase 评测集** `scripts/eval_badcase.py` **42/42**：8 组历史踩坑（判分误判/LaTeX 残渣/
   模板排版/路由优先级/记忆隔离/压缩瘦身/安全/结构化输出/视觉分离/Redis 降级/内容审核）。
   注意：判分用例用 `answers_equal` 而非 `check_answer`（后者查库）；Config 只读，
   改配置用 `cfg.raw`；库内 \uXXXX 是 JSON 转义需先剥除再扫 LaTeX。
4. **Redis 短期记忆**：`docker-compose.yml`（redis:7-alpine，AOF+LRU+healthcheck）；
   requirements 加注释行；RedisStore 早已实现，补了"连不上优雅回退内存"验收（34 条）。
5. **未成年人保护与内容审核** `safety.py`（纯规则、离线生效）：
   - check_input：暴力/自伤/性/违法/代写作弊/隐私探询 6 类词表拦截，返回引导话术（含 12355），
     builder._invoke_locked 入口拦截，不进主流程；词表**只用词组**——「入侵他人」没匹配到
     「入侵别人」的教训：覆盖同义词（他人|别人|系统|网站|服务器|账号）；
   - mask_pii：手机/身份证/邮箱/QQ 微信/住址（带路号等后缀）/学校班级（带年级班）；
   - sanitize_output：回复脱敏 + 索取隐私话术替换。
- 全量回归（LLM 在线）：34/34 + 11/11 + 19/19 + 21/21 + 16/16 + **42/42** + 7 passed。
- README：待办五项全部勾掉并附实现位置；快速开始 11 步；清单加 /api/traces、/trace、
  验收七套、硬约束 9/10。

## 14:30 Web 界面视觉升级 + 数学公式渲染（KaTeX 本地化）
- 用户需求：优化 web 视觉、解决数学符号显示为字符的问题。
- **KaTeX 本地化（离线可用）**：从 npmmirror 下载 katex-0.16.11 tgz，dist 解压到 `web/katex/`
  （3MB 含字体）；server.py 新增 `app.mount("/static", StaticFiles(directory=WEB_DIR))`——
  此前只有 `/` 和 `/trace` 两个 HTMLResponse，没有静态目录挂载。
- **渲染管线（web/index.html）**：题库是纯文本记法（x^2、√(7)-1、(a+b)/(2)、△ABC∽△DEF），
  管线 = 切分数学片段（CJK 边界 + 数学符号集）→ texify 转 LaTeX → \( \) 包裹 → auto-render；
  原文已含 $...$/\frac 等 LaTeX 时直接走 auto-render。关键细节：
  - UNI_MAP 字母命令**必须带尾随空格**（`\triangle` 粘连 `ABC` → `\triangleABC` 未定义命令）；
  - 先 esc 再 texify（浏览器里 auto-render 读解码后的 textNode，`&lt;` 不是问题——
    node 直测 KaTeX 时要手动 unescape 才等价于浏览器行为）；
  - 数学片段判定：含运算符/数学符号才进公式，纯单词（AB/LangGraph/单独数字）保持原文；
  - 流式输出期间先显示纯文本，SSE 结束后整体 formatAI+renderKatex。
- 冒烟测试 `scripts/test_math_render.js`：从 index.html 抽取纯函数段 eval + 本地 katex.min.js
  实渲染，10 用例全过（真实题干 + LLM LaTeX）。
- 视觉升级：渐变 header/头像气泡/入场动画/分段式 tab/学习模式紫罗兰主题色
  （body.learn 切换 --brand）/快捷 chip 悬浮/自定义滚动条/状态徽章脉冲点。
- 验证：`/static/katex/*` 全 200；/api/chat 端到端正常（LLM 在线 ~30s）。

## 15:20 前端自动化检测（playwright-core + 本机 Edge，16/16 通过）
- 环境坑：agent-browser CLI 安装成功但 `agent-browser install`（Chromium 从
  storage.googleapis.com 下载）失败、`connect` CDP 也挂起——**不可用**。
  可用方案：`npm i playwright-core` + `executablePath` 指向本机
  `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`（Win 自带 Edge，免下载）。
- 检测脚本固化在 `scripts/frontend_check.js`（16 项）：页面加载/KaTeX 库/欢迎消息/
  运行状态面板/公式渲染（分数√上标△∽、无 ^ 残留）/学习模式切换+主题色/知识点下拉/
  真实 LLM 对话流/控制台零错误。
- **KaTeX 检测口径**：`^` 出现在 textContent ≠ 残留——KaTeX 在隐藏的
  `<annotation encoding="application/x-tex">` 里存原始 TeX 源码；要查**可见层**
  `.katex-html` 的 textContent。
- **favicon data URI 必须 URL 编码**：`href="data:image/svg+xml,<svg...>"` 里的 `<`/`>`
  在被预览工具注入属性后解析崩坏，页面左上角泄漏 `'/>'` 文本；改成
  `%3Csvg ...%3E` 编码形式后正常。📐 = %F0%9F%93%90。
- 截图确认：学习模式紫罗兰主题、公式（(1+√1994)/2 分数、(4x³-1997x-1994)^2001、
  △ABC∽△DEF、kx²-x+1=0）渲染完美。

## 15:58 Web 作答面板 + 数学符号快捷键（11/11 E2E 通过）
- 需求：应用题作答面板 + 数学特殊符号快捷键。
- 实现（web/index.html，无后端改动）：
  - **符号栏常驻**输入框上方（26 个：∠△∽≌⊥∥√()x²x³x_n°′≤≥≠≈±×÷∵∴πΔ 分数模板()/()
    括号模板（　）:），点击插入到**光标处**（selectionStart/End）；
  - **作答面板**：多行 textarea + 清空/提交按钮，`√ 作答`按钮或出题时自动弹出，
    Ctrl+Enter 提交，提交即走 send() 主流程（判分/记录不变）；
  - 自动弹出正则：`第N题|请作答|请解答|写出…(过程|答案)|（　）|()|填空题|应用题|选择题|解答题`。
- **设计教训**：符号栏最初藏在面板内，面板收起就没法用——改为常驻，
  插入目标 = 面板 textarea（展开时）否则主输入框。
- E2E（playwright-core + Edge，panel_check.js）：符号插入光标、收起时插主输入框、
  面板提交、真实 LLM 出题→自动弹出→分数模板作答→判分回复、无 JS 错误，11/11。
- 保留 SYMS 数组格式 [标签, 插入文本, 光标回退位数]，加符号只需改一处。

## 16:55 接入 SXT001_CN 题库数据源（题库 2009→2034）
- 用户要求增加知识库 `modelscope.cn/datasets/market.aliyun/SXT001_CN`
  （阿里云市场数学题库：初中约 11990 题）。**关键发现：仓库仅含公开样例**
  （初中 12 个 json、高中 12 个，每文件 1 题），完整数据需阿里云市场购买。
- 下载 URL：`https://www.modelscope.cn/api/v1/datasets/market.aliyun/SXT001_CN/repo?Revision=master&FilePath=...`
  （**必须 -L 跟随重定向**，否则 0 字节）；目录树用 `/repo/tree?Revision=master&Root=`。
- 字段：科目/答案([["A"]])/年级/一级知识点/题型(["", "单选"])/题干(HTML+$$LaTeX)/
  选择(字符串化 list "A : <p>...</p>")/分析(字符串化 list)/难易度/文件名称。
- ETL 集成（build_question_bank.py 新增 `load_sxt001()`）：
  - `ast.literal_eval` 解字符串化 list（_sxt_unwrap 递归）；
  - **HTML 实体（&there4;→∴）和 `^\circ`→° 必须对题干/选项/分析三处都做**
    （首次只处理题干，选项残留 "60^°"、解析残留 "there4;"）；
  - 复用 latex_to_text/clean_text；题干记法转 (a)/(b)、√(3) 与全库一致；
  - question_id 前缀 `SXT_`（C/F 派生），source='SXT001_CN'。
- 重建：题库 2009→2034 条（SXT 12 题派生 25 条），向量 2034 条；
  板块分布 函数 519 / 二元 572 / 几何 559 / 综合 384，均 ≥200。
- 回归：badcase 42/42（含 LaTeX 残留扫描）、optimizations 34/34、pytest 7 passed。
- 后续扩容：完整数据下载后放 `data/raw/sxt001/` 重跑 build_question_bank.py 即可。





## 知识点体系扩容到完整初中大纲（6 板块 38 知识点）
- teaching.py CURRICULUM 重构：新增「数与式 6 / 统计与概率 4 / 锐角三角函数 4」，
  「二元一次方程」改名并扩为「方程与不等式 8」，「几何图像」改名「几何 10」；
  函数 6 不变。合计 6 板块 38 知识点（每个含定义/要点/易错点/口诀）。
- 关键坑：_P_TOPIC 顺序敏感——「锐角三角函数」必须排在「函数」「几何」之前
  （含"函数""三角形"字样会被劫持）；「方程与不等式」排在「数与式」之前
  （分式方程会被"分式"劫持）。
- pick_examples 新增讲解板块→题库板块映射 _EXAMPLE_TOPIC（题库 topic 仍是
  函数/二元一次方程/几何图像/综合，不动题库）；"综合"在 fetch_questions 里
  等于不限板块（全库随机）。
- 同步改：knowledge_tutor 结课话术、tools/teaching.py 参数描述、
  eval_teaching 断言（板块数==6、8 条 match_topic 用例、新板块路由）。
- quiz/刷题板块口径（nodes.py/quiz.py/repository.py 的 TOPICS）不变。
- 验收：eval_teaching 19/19、eval_conversation 11/11、eval_badcase 42/42、
  eval_optimizations 34/34、pytest 7 passed；服务重启后 /api/curriculum 返回 6 板块。

## 技术文档
- 产出 `技术文档.md`（项目根目录，Markdown）：架构图/图节点/目录结构/数据层(2034题三来源)/
  API 10 接口/Web 前端(KaTeX管线+符号快捷键+作答面板)/安全/可观测性/部署/7套验收基线/
  8 条代码层硬约束清单/后续方向。
