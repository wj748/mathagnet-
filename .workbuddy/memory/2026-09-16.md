- 应用户要求新增《技术文档-架构详解.md》（不覆盖 v2.0 总览）：七节结构——文件逻辑（模块职责+调用链+数据落地）、11 工具清单（requires/retryable/timeout）、MathAgentState 分组+跨轮生命周期（_RESET/carried 六键/持久化）、9 节点职责、连线与路由映射、六层预防机制全清单（safety 三层/registry 三重防护/toolguard 十项/ReAct 四道闸/记忆有界/并发鉴权）、补充（路由 12 级优先级、API 端点、LLM 双模降级矩阵、评测基线与边界声明）。内容全部经源码逐文件核实。
- **Web 界面视觉优化（web/index.html）**：设计令牌体系扩充（--panel-2/--soft/--field/--ai-bubble/--scroll/--glow/--radius）；头部玻璃拟态（backdrop-filter）+ 渐变标题字 + logo 悬停微旋转；聊天区点阵底纹 + 气泡阴影分级；按钮/芯片/符号键悬停抬升与按压反馈；新增自动深色模式（prefers-color-scheme 全量变量切换）+ prefers-reduced-motion 动效关怀 + theme-color 双主题 meta。纯 CSS 为主，JS 业务逻辑零改动。
- **顺手修掉前端既有竞态**：/api/session 握手挂成 sessionReady Promise，loadStats await 后再拉统计（此前 token 未到就 401 → 状态面板误报"服务未启动"）。
- **frontend_check.js 同步修正**：模式徽章断言对齐学生化文案（'AI 老师在线'/'基础模式'）；切回刷题页签后先等发送按钮恢复再点快捷指令（否则「退出学习」占用同用户互斥闸 → 429 busy 拿不到真实回复）。修后 16/16 全绿（真实 LLM 链路）。
- **环境备忘**：scripts/node_modules 已建 junction → .workbuddy/tmp/pwtest/node_modules（playwright-core），frontend_check.js 可直接 node 运行；cmd //c mklink 在 Git Bash 下会失效，必须用 PowerShell New-Item Junction。
- **显示屏自适应改造（web/index.html）**：桌面端（≥901px）改 app-shell 布局——body overflow:hidden、wrap 100vh flex 列、grid 弹性伸展（侧栏 clamp(264px,23vw,320px)）、#chat 改 flex:1 弹性伸缩（弃固定 min(60vh,600px)）、侧栏内部独立滚动；≥1600px 大屏整体放大字号；≤780px 高的小笔记本压缩留白与 logo；≤900px 窄屏回落文档流（原样式）。实测 1366×768 / 1920×1080 / 2560×1440 全部零页面滚动、聊天区随视口伸缩，390×844 移动端自然滚动。frontend_check 16/16 复验通过。
- **项目清理（用户确认 A+B 后执行）**：17 项共 64.4MB 送 Windows 回收站——edge-profile 58MB、katex.tgz+package 5.2MB、.pytest_cache、pwtest 旧截图×7、旧补丁脚本×2、pwtest 旧 js 副本×2、data/cleanup_backup_20260910。保留：pwtest/node_modules（frontend_check 依赖）、data 全部、文档全部。**工具链**：PowerShell Add-Type 与 COM New-Object 均被沙箱拦截，回收站删除用隔离 venv 的 send2trash（pip install send2trash 已装）。
