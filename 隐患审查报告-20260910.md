# 隐患审查报告（2026-09-10）

> 审查方式：三条线并行静态审查（API/安全/前端、图路由/工具/记忆、数据层/教学/基础设施），关键发现已逐条人工核实代码。
> 上一份报告：`隐患审查报告-20260908.md`。本次共发现 **3 条 P0、6 条 P1、13 条 P2**，其中 3 处笔误已当场修复。

---

## 一、P0 —— 功能性缺陷（新板块引入的回归 + 模式切换漏洞）

### P0-1 「刷概率题/刷统计题/刷三角函数」在规则路由下直接失效 ✅已核实
- `graph/router.py:24-26`：`_P_QUIZ` 只含 `刷函数|刷方程|刷几何`，不含概率/统计/三角函数 →「刷概率题」不匹配任何意图，落到兜底闲聊。
- `graph/nodes.py:18-22,47`：`parse_quiz_request` 板块映射同样缺失 → 即使进 quiz，「刷统计」静默退到「综合」（全库拉无关题）；「刷三角函数」因含"函数"被**误判为函数板块**（题库无三角函数题，答非所问）。
- **修复建议**：`_P_QUIZ` 增加 `刷概率|刷统计|刷三角|概率题|统计题`；nodes 映射加 `统计与概率/锐角三角函数` 条目（放在"函数"之前），无法落库的板块显式提示"该板块题库暂未收录"。同时把这三句话补进 `eval_conversation.py`。

### P0-2 学习模式下切刷题后，作答被学习会话吞掉 ✅已核实
- `router.py:43-48`：`mode=="learn"` 时非 concept/quiz/grade/habit/submit 一律返回 `learn`。用户在学习页签输入"我要刷函数的选择题"→ 正确切到 quiz；但下一轮作答 "A"（页签仍是 learn）→ 被路由到 `knowledge_tutor`，quiz 会话永远收不到这个答案。
- 评测未覆盖此场景（`eval_conversation.py` 只测切换那一次）。
- **修复建议**：quiz 意图触发时清 `session_mode`（builder `route_by_intent` 后强制 `session_mode=""`），或 learn 分支内优先判定 `in_quiz` 短输入作答。

### P0-3 OCR"确认批改"二次确认跨轮失效 ✅已核实
- `graph/builder.py:111,141-142`：`_RESET` 把 `ocr_payload` 置 None，而跨轮 `carried` 键列表只有 `tool_call_history/short_term_memory/quiz_session/learning_session`，不含 `ocr_payload`/`grade_result` → 低置信度 OCR 提示"回复确认批改"后，下一轮 payload 为空，陷入"请把路径发给我"死循环。
- **修复建议**：把 `ocr_payload`、`grade_result` 加入 `carried`。

---

## 二、P1 —— 高优先级

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| P1-1 | `teaching.py:102` | 一元二次方程定义写错：**Δ=b²-4c**（应为 b²-4ac），与同条要点自相矛盾——教学硬伤 | ✅**已当场修复**，teaching 回归 19/19 |
| P1-2 | `data/.secret_key` | HMAC 签名密钥落盘但 **.gitignore 未覆盖**（`.env` 在但密钥文件不在），一旦 git init 即泄露 | ✅**已加 .gitignore**；建议改由 .env 注入并轮换 |
| P1-3 | `server.py:190` | 上传接口 `read(MAX+1)` 一次性读入内存再判大小，内存 DoS 防护形同虚设 | 分块流式读取，超限即断返 413 |
| P1-4 | `builder.py:123 + server.py:196` | 安全审核只覆盖文本消息：**OCR 提取的学生作答内容不进 `check_input`/`mask_pii`** 就送 LLM 与记忆 | OCR 返回后先审核再入流程 |
| P1-5 | `safety.py:24-67` | 审核词表/PII 正则均为精确匹配，`自 杀`、`自鲨`、`薇信`、全角ＱＱ 等变体全部绕过；反之 17-18 位长数字数学答案会被误打码成身份证 | 匹配前归一化（去空格/全角转半角/常见谐音表）；纯数字串加语境白名单 |
| P1-6 | `grading.py:69-122` | 判分不剥离单位：`text_equal("5cm","5")` → False 误判错（数值等价 0.5=1/2 的 sympy 路径是对的）；标准答案"1或-1"类多答案也不支持 | 数值型先做单位归一；含"或/、"的标准答案做集合匹配 |

---

## 三、P2 —— 值得排期

**架构/体验类**
1. `builder.py:77`：`InMemorySaver` checkpointer——进程重启全部进行中的 quiz/learn 会话丢失（长期记忆在 Qdrant 可恢复）。建议换 SqliteSaver/Redis。
2. `trace.py/metrics.py`：观测数据纯内存，重启归零，无法做跨重启趋势分析。建议落 SQLite 或定时快照。
3. `server.py:143-168`：**SSE 是伪流式**——整轮算完再逐字 yield，首字节延迟=整轮耗时；无 `request.is_disconnected()` 检测，客户端断开 LLM 仍跑完；`web/index.html` 端无断线重连、单事件无 JSON 容错。建议接入 LLM token 级流式 + 断连检测。
4. `llm.py`：单次调用有 60s 超时（不会无限挂），但**无请求级总超时、无重试**；一轮最坏触发路由+≤8 次 ReAct+判分+分析多次调用。建议加总 deadline + 有限重试退避。
5. `nodes.py:158-183`：出题**无去重**（从不传 `exclude_ids`，"再刷3道"重复出题）、无难度自适应。建议基于 user_history 做错题排除与难度递进。
6. `nodes.py:116-119`：刷题中**任何非空文本都当答案判分**——"这道题好难"会被判错。建议仅选项/纯数值/短 token 视为作答。
7. `react.py`：信息增益终止阈值 0.80 可能误停；工具失败直接 finish 无重试；无整体壁钟超时。
8. `knowledge_tutor.py:150-162`：学习中自由提问强行回显当前知识点要点，无关问题答非所问；`find_point_index` 子串匹配"分式"⊂"分式方程"会误命中（teaching.py:422）。

**安全/运维类**

9. `server.py:193-200`：上传文件**从不清理**，磁盘无限增长；`need_confirm` 靠 `"识别置信度" in response` 字符串判断，脆弱。
10. `server.py:133`：500 错误把 `异常类型: 详情` 直接返回给客户端，内部信息外泄；upload 路由甚至未捕获。
11. `server.py:230-256`：`/api/metrics`、`/api/traces`、`/trace` **始终无鉴权**（即使 require_token=true），运维数据裸奔。
12. token 以 URL query 明文传输（会进访问日志/代理日志）；TTL 30 天偏长且无吊销。建议改 `Authorization: Bearer`。
13. `vectorstore.py`：Qdrant 本地嵌入式单进程锁（多 worker 部署会冲突，需换 server 模式）；`search_knowledge` 未应用 `score_threshold`，RAG 可能注入低相关片段；BGE 降级 hashing 时无告警指标、health 不显式暴露 `embedder=fallback`。

---

## 四、P3 / 仓库卫生

- `web/index.html:485`：符号栏 `x_n` 实际插入 `_1`——✅**已当场修复**（`_1`→`_n`）。
- 根目录 `test.json`、`test2.json` 为 SXT001 下载测试残留（内容相同）——建议删除（属用户文件，未代删，已加 .gitignore）。
- `data/` 下 13 个 `*.log`、`.idea/` 未被忽略——✅**已加 .gitignore**（`*.log`、`.idea/`）。
- `data/processed/questions.sqlite` 全仓无代码引用，疑为旧版 ETL 遗留副库，建议确认后删除。
- `data/uploads/vision_test.png` 测试残留。
- `.gitignore` 原本不覆盖 `data/processed/`（重建产物），建议视需求补充。

## 五、本次抽查确认无问题的项（避免误伤）

- 38 个知识点数学表述抽查（特殊角函数值/韦达定理/方差/扇形面积/抛物线顶点）全部正确，唯一硬伤是已修复的判别式笔误。
- 前端 XSS：所有插入点均走 `esc()`/`textContent`，KaTeX 未开 `trust`，未发现注入点。
- 上传文件名路径穿越防护正确；SQLite WAL+busy_timeout+写重试已正确生效；`learning_session` 跨轮保留正确；题目重建 `reset_questions()` 不会误删用户答题记录（但会留下孤儿 attempt 引用，见 P2-13 邻近建议）。

---

## 六、修复优先级建议（路线图）

1. **立即**（半小时）：P0-1/2/3 + P1-6 判分单位归一——都是小改动，但直接影响核心体验；修完补 eval_conversation 用例（新板块路由、学习切刷题作答）防回归。
2. **短期**：P1-4 OCR 内容审核、P2-9/10（上传清理 + 错误信息收敛）、P2-5 出题去重。
3. **中期**：SSE 真流式 + 断连检测（P2-3）、checkpointer 持久化（P2-1）、安全归一化层（P1-5）、监控接口鉴权（P2-11）。
4. **长期**：题库补充统计/三角函数板块、trace/metrics 持久化、LLM 请求级超时治理。

---

## 七、修复实施记录（2026-09-10 当天完成）

| 项 | 状态 | 改动位置 |
|---|---|---|
| P0-1 新板块刷题路由 | ✅ 已修复 | router.py `_P_QUIZ` 加 概率/统计/三角；nodes.py `_P_TOPIC` 加两个新板块（置于"函数"前）+ `_NO_BANK_TOPICS` 明确提示回退综合 |
| P0-2 学习切刷题作答被吞 | ✅ 已修复 | router.py learn 分支加 `and not in_quiz`，刷题会话中的作答正常回 quiz 判分 |
| P0-3 OCR 二次确认死循环 | ✅ 已修复 | builder.py `carried` 加入 `ocr_payload`、`grade_result` |
| P1-5 安全归一化 | ✅ 已修复 | safety.py：全角→半角、去分隔符、谐音变体（自鲨/薇信/v信/绿泡泡等）；privacy_probe 补反向探词；PII 正则加数学运算符守卫（算式内长数字不打码） |
| P1-6 判分单位/多答案 | ✅ 已修复 | quiz.py：`_strip_unit` 单位剥离（cm/千米/°/万元等）；`_multi_equal` 多答案（"或"任选其一、"、"需答全顺序无关） |
| P1-4 OCR 内容审核 | ✅ 已修复 | ocr.py：提取文本/blocks 出工具前统一 `mask_pii` |
| P2-5 出题去重 | ✅ 已修复 | nodes.py：新会话透传 `exclude_ids`（排除上轮已出题），拉空自动放开重试 |
| P2-6 刷题中闲聊判错 | ✅ 已修复 | nodes.py：情绪表达（好难/太难/求助等）给引导话术，不当答案判分 |
| P2-9 上传清理 | ✅ 已修复 | server.py：OCR 后 `finally unlink`；need_confirm 改用结构化字段 |
| P2-10 错误信息收敛 | ✅ 已修复 | server.py：chat/stream/upload 异常只记日志，对外统一"服务内部错误" |
| P2-11 监控接口鉴权 | ✅ 已修复 | server.py：require_token=true 时 /api/metrics、/api/traces、/trace 需有效 token |
| P2-13 备注 | 部分更正 | 经核实 `file.read(MAX+1)` 实际受 SpooledTemporaryFile 限制，内存有界，原 P1-3 降级为"可选优化" |

**评测用例补充**（eval_conversation.py）：新板块路由 4 条（刷概率/统计/三角函数）、
学习切刷题后作答 2 条（P0-2 回归）、学习中小测作答 1 条。

**全量回归（LLM 在线）**：路由 38/38 · 学习路由 13/13 · 行为约束 11/11 · 判分 34/34 ·
记忆 21/21 · 安全 16/16 · badcase 42/42 · 教学 19/19 · pytest 7 passed。服务已重启验证。

**遗留（中期项，未实施）**：SSE 真流式+断连检测、checkpointer 持久化、LLM 请求级总超时/重试、
react 超时与重试、trace/metrics 持久化、统计与概率/锐角三角函数题库定向补充。
