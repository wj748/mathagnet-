# 智能助教 · 架构详解文档

> 版本：v1.0 ｜ 日期：2026-09-16 ｜ 依据：`src/math_agent` 源码逐文件核实（非回忆式描述）
> 定位：与《技术文档.md》（v2.0 项目总览）互补，聚焦「文件逻辑 / 工具 / 状态 / 节点 / 连线 / 预防机制」六维度的实现细节。

---

## 1. 文件逻辑

### 1.1 目录总览与职责边界

```
智能助教/
├── src/math_agent/                 ← 核心包（所有业务逻辑在此）
│   ├── state.py                    全局状态 TypedDict 定义 + new_state() 工厂
│   ├── config.py                   cfg 单例：读 config.yaml + .env 覆盖
│   ├── llm.py                      LLM 门面：available/chat/chat_json（无 Key 全降级）
│   ├── safety.py                   未成年人保护：输入审核 / PII 脱敏 / 输出审核
│   ├── toolguard.py                工具失败治理：分类/重试/熔断/超时/步骤台账
│   ├── metrics.py                  可观测性：工具成功率/意图分布/循环终止/守卫计数
│   ├── trace.py                    调用链追踪：每轮 record_turn（脱敏后落盘）
│   ├── concurrency.py              并发闸门（进程内信号量，同用户串行化在 builder）
│   ├── embeddings.py               BGE-small-zh-v1.5；无 torch 回退 hashing 向量
│   ├── vectorstore.py              Qdrant 本地嵌入式封装（双 Collection 隔离）
│   ├── chunking.py                 题库文本切片（入库前）
│   ├── analysis.py                 错因分析规则（quiz_session records → 错因分类）
│   ├── teaching.py                 课程数据 CURRICULUM + 讲解文本构建（代码层渲染骨架）
│   ├── topics.py                   板块命名单一真源：BANK_TOPICS / TEACHING_TO_BANK
│   ├── db/
│   │   ├── models.py               SQLAlchemy 模型（questions/attempts/agent_states…）
│   │   └── repository.py           数据访问层：题库增删查/判分记录/会话状态持久化
│   ├── memory/
│   │   ├── short_term.py           ShortTermMemory：Redis 启用即分布式，否则进程内
│   │   └── compressor.py           分层记忆：实体锚/近期窗口/要点压缩/prune_state
│   ├── graph/                      ← LangGraph 层（§4 节点 / §5 连线）
│   │   ├── builder.py              build_graph() + MathAgent 门面（invoke 生命周期）
│   │   ├── router.py               意图路由：12 级规则正则 + LLM 兜底
│   │   ├── nodes.py                6 个业务节点（router/quiz/grading/analyzer/memory/habit）
│   │   ├── knowledge_tutor.py      学习模式节点（讲→练→测状态机）
│   │   └── react.py                ReAct 节点：Thought/Action/观察 + 双重终止 + 失败治理
│   ├── tools/                      ← 工具层（§2 工具）
│   │   ├── registry.py             注册中心：Pydantic 校验/依赖链/防重/守卫执行入口
│   │   ├── calc.py                 calculator（SymPy 精确计算）
│   │   ├── quiz.py                 fetch_quiz / check_answer
│   │   ├── grading.py              grade_assignment / grade_application_question
│   │   ├── ocr.py                  ocr_document（多模态，timeout=90s）
│   │   ├── memory_tools.py         search_knowledge / search_user_memory / update_long_term_memory
│   │   ├── report.py               generate_error_report（骨架代码渲染，LLM 只追加）
│   │   └── teaching.py             explain_knowledge_point（讲解骨架代码渲染）
│   └── api/
│       ├── auth.py                 /api/session 签发 HMAC token（客户端自报 user_id 不可信）
│       └── server.py               FastAPI：10 个端点（§7.2）
├── web/index.html                  前端单页（KaTeX 渲染 / SSE 流式 / 模式切换）
├── scripts/                        构建与评测脚本（7 套 eval + build_question_bank 等）
├── tests/                          pytest 冒烟（26 passed + 6 skipped 基线）
├── data/                           SQLite 题库 / Qdrant 向量库 / 上传白名单目录
└── config.yaml                     图参数 / toolguard 参数 / Redis 开关等运行配置
```

### 1.2 一次请求的文件调用链

```
web/index.html
  → api/server.py（auth.py 校验 HMAC token → _agent(user_id) 取/建 MathAgent）
    → graph/builder.py MathAgent.invoke()
      ├── safety.check_input()                 # 输入审核，不通过直接返回
      ├── memory/compressor.prune_state()      # 上一轮状态裁剪后回注
      ├── compressor.build_context()           # 压缩上下文注入 conversation_digest
      ├── graph.invoke()                       # LangGraph 状态机（§4/§5）
      │     └── 节点内 → tools/registry.run_tool()
      │           └── toolguard.execute_with_guard()   # 熔断→超时→分类重试→脱敏
      │                 └── 具体工具 → db/repository / vectorstore / llm
      ├── safety.sanitize_output()             # 输出审核
      ├── compressor.add_turn() ×2             # 对话回写分层记忆（PII 已脱敏）
      └── trace.record_turn() + _persist_state()  # 追踪落盘 + 跨轮状态持久化
  → JSON / SSE 返回前端
```

### 1.3 数据落地位置

| 数据 | 位置 | 生命周期 |
|---|---|---|
| 题库（2034 题） | SQLite（`db/repository.py`）+ Qdrant `knowledge_base` | 全局只读 |
| 用户长期记忆 | Qdrant `user_long_term_memory`（强制 user_id 过滤） | 用户私有，可 reset |
| 判分/刷题记录 | SQLite attempts / quiz_sessions | 用户私有 |
| 跨轮会话状态 | SQLite `agent_states`（`_PERSIST_KEYS` 六键快照） | 进程重启可恢复 |
| 对话分层记忆 | ShortTermMemory（Redis `math:conv:*` 或进程内） | 滑动窗口 + 摘要 |
| 步骤台账 | `math:task:{task_id}`（TTL 1h，最近 32 步） | 任务级 |
| 调用链 trace | trace 模块（输入/回复脱敏后） | `/api/traces` 查询 |

---

## 2. 工具

### 2.1 工具清单（11 个，均注册于 `tools/registry.py`）

| 工具名 | 文件 | 功能 | requires 依赖 | retryable | timeout |
|---|---|---|---|---|---|
| `calculator` | calc.py | SymPy 精确计算表达式/解方程/求导 | — | ✓ | 30s |
| `fetch_quiz` | quiz.py | 按板块/题型/难度/数量拉题 | — | ✓ | 30s |
| `check_answer` | quiz.py | 客观题判分（只判对错，不做错因分析） | — | ✓ | 30s |
| `grade_assignment` | grading.py | 整卷批改：OCR 文本逐题切分判分 | `ocr_document` | ✓ | 30s |
| `grade_application_question` | grading.py | 应用题过程给分（知识库比对优先，LLM 兜底） | — | ✓ | 30s |
| `ocr_document` | ocr.py | 图片/PDF/文本 → 纯数学题目文本 | — | ✓ | **90s**（多模态放宽） |
| `search_knowledge` | memory_tools.py | 全局题库/概念语义检索（retrieval.score_threshold 内置） | — | ✓ | 30s |
| `search_user_memory` | memory_tools.py | 用户长期记忆检索（**代码层强制 user_id 隔离**） | — | ✓ | 30s |
| `update_long_term_memory` | memory_tools.py | 写入一条学习习惯/薄弱点 | — | **✗**（写类，幂等保护） | 30s |
| `generate_error_report` | report.py | 错题报告（骨架代码渲染，LLM 仅追加「老师建议」） | — | ✓ | 30s |
| `explain_knowledge_point` | teaching.py | 知识点讲解（◆定义/要点/易错点/口诀骨架代码渲染） | — | ✓ | 30s |

### 2.2 注册中心三重防护（`registry.run_tool` 统一入口）

执行顺序：**校验 → 依赖链 → 防重 → 守卫执行**，任一步失败都返回结构化 `ToolResult`（`ok / name / error / blocked / args_hash`），错误文本经 `sanitize_error` 脱敏后才回注。

1. **结构化参数校验**：每个工具绑定 Pydantic v2 `args_model`，非法参数直接拒绝并回注正确 schema——LLM 传错参拿不到「半执行」结果。
2. **依赖链约束**：`requires` 检查历史中是否已**成功**调用前置工具（如 `grade_assignment` 必须 `ocr_document` 在前），顺序违规在代码层拦截。
3. **防重拦截**：`args_hash = md5(tool_name + 校验后参数)` 命中历史即拒绝（`blocked=True`），回注「已用相同参数调用过」——防 LLM 同参空转。

防重语义在记忆压缩后依然成立：`prune_state` 压缩 `tool_call_history` 时保留 `name + args_hash + ok` 最小字段集。

---

## 3. 状态

### 3.1 `MathAgentState`（`state.py`，TypedDict，`total=False`）

| 分组 | 键 | 说明 |
|---|---|---|
| 对话 | `messages` `user_input` `user_id` | 本轮输入 + LangChain 消息列表 |
| 意图 | `intent`（quiz/submit/grade/habit/learn/chat）`intent_confidence` | 由 router 节点写入 |
| 刷题会话 | `quiz_session`（questions/index/records/topic/type）`pending_answer` `submit_event` `awaiting_user` | `submit_event` 是代码层硬约束的交卷标记，Error Analyzer 只认它 |
| 批改 | `ocr_payload` `grade_result` | OCR 文本与判分结果 |
| 学习模式 | `session_mode`（''/learn）`learning_session`（topic/index/stage/pending_quiz/correct/total） | 讲→练→测状态机载体 |
| 记忆 | `short_term_memory`（含 observations 滑窗）`long_term_memory_hits` `memory_written` | |
| 报告 | `error_report` `final_report` `_records` | `_records` 为批改流程待分析记录 |
| ReAct 控制 | `react_loop_count` `react_thoughts` `last_result_vector` `loop_stop_reason`（max_loop/information_gain/finished/fatal_error/degraded）`task_id` `tool_replan_count` `react_forced_action` | 循环终止与失败治理控制面 |
| 工具 | `tool_call_history`（防重/依赖链判定源）`tool_blocked_notice` | |
| 其它 | `response` `trace` `conversation_digest`（压缩上下文注入） | |

`new_state()` 工厂为全部键给初值，节点内不需要到处 `.get()` 判空。

### 3.2 跨轮状态生命周期（`builder.py MathAgent`）

这是全项目最容易被误解的一块，规则如下：

1. **真正持久的是 checkpointer 里的 state**；每轮 `invoke` 的 payload 是新 dict。
2. 每轮开始：payload 先套 `_RESET`（清掉 17 个一次性键），再从 `last_state` 取 **carried 六键**（`tool_call_history / short_term_memory / quiz_session / learning_session / ocr_payload / grade_result`）→ 经 `prune_state()` 裁剪 → **显式回注**。不回注 = 旧值在 checkpointer 里原样存活，裁剪等于没做。
3. `conversation_digest`：由 `compressor.build_context()` 生成（实体锚 + 历史要点 + 近期原文），注入供 ReAct prompt 使用；轮次结束 `add_turn()` 回写。
4. 每轮结束 `_persist_state()`：只落 `_PERSIST_KEYS` 六键快照到 SQLite `agent_states`——大对象/临时产物不入库，防无限膨胀；进程重启 `_restore_state()` 恢复，失败降级为空。
5. 同用户并发：`invoke()` 持 `RLock` 串行化（checkpointer / last_state 不可重入）。
6. `reset()`：换新 checkpointer + 清 `agent_states` 快照 + 清压缩任务——三处都清才算真重置。

---

## 4. 节点

图共 **9 个节点**（8 业务 + 1 finalize 空节点）。入口统一为 `intent_router`。

| # | 节点 | 文件 | 职责 |
|---|---|---|---|
| 1 | `intent_router` | nodes.py | 调 `router.route_intent` 写入 `intent`；无 LLM 时 12 级正则规则全覆盖（见 §7.1 优先级） |
| 2 | `quiz_manager` | nodes.py | 刷题状态机：解析出题请求（板块/题型/数量/难度正则）→ `fetch_quiz` → 出题；刷题中短输入作答 → `check_answer` → 分层提示（3 级 hint）→ 单题解析；识别交卷置 `submit_event` |
| 3 | `grading` | nodes.py | 批改链：消费 `ocr_payload`（或先触发 OCR）→ `grade_assignment` 逐题判分 → 产出 `_records` |
| 4 | `error_analyzer` | nodes.py | 只认 `submit_event`/交卷；调 `generate_error_report` + `analysis.py` 错因分类 → 写 `error_report` |
| 5 | `memory_manager` | nodes.py | 记忆读写：`update_long_term_memory` 写习惯 + `search_user_memory` 检索注入；置 `memory_written` |
| 6 | `habit` | nodes.py | 学习习惯报告：读取历史 records + 长期记忆 → 结构化报告（骨架代码渲染） |
| 7 | `knowledge_tutor` | knowledge_tutor.py | 学习模式状态机：选板块 → `explain_knowledge_point` 讲解 → 例题 → 小测（`fetch_quiz`）→ `check_answer` 判分讲评 → 下一个知识点；支持「下一个/没听懂/换个例题/A-D 作答/退出」指令 |
| 8 | `react` | react.py | ReAct 循环体（每轮一个节点调用）：Thought → 双重终止判定 → Action（LLM 决策或规则计划）→ `run_tool` → 观察入 `short_term_memory.observations` → 台账记录 → 失败治理（§6.3） |
| 9 | `finalize` | builder.py | 空汇聚节点（`lambda s: {}`），所有分支的统一出口，连 END |

**react 节点内部步骤**（单轮）：
```
0) task_id 兜底生成 + 首轮 ledger_create
1) Thought 生成（LLM 或规则轨迹）→ embed_one 得向量
2) 终止判定：与上轮向量余弦 ≥0.80 → information_gain；
   轮数 ≥ max_react_loops(8) → max_loop；命中则 ledger_finish + 出答案返回
3) Action：优先执行 react_forced_action（上轮失败的重规划强制动作，用后即清），
   否则 LLM JSON 决策 / 规则计划（检索→计算→主题细化）
4) run_tool（守卫链）→ record_history → 观察入 obs → ledger_record
5) 失败治理：blocked/param 计入 replan 上限(2)；fatal 立即降级；
   超限降级出部分结果；配置了 fallback_tools 则置强制动作
```

---

## 5. 连线

### 5.1 边定义（`builder.build_graph`）

```python
entry → intent_router
intent_router ──(route_by_intent 条件边)──► quiz_manager | error_analyzer | grading
                                           | habit | knowledge_tutor | react
quiz_manager    → finalize                                    # 固定边
grading         ──(after_grading)──► error_analyzer | finalize # 有 grade_result 才进分析
error_analyzer  ──(after_analyzer)──► memory_manager | finalize # 有 error_report 才写记忆
memory_manager  → finalize                                    # 固定边
habit           → finalize                                    # 固定边
knowledge_tutor → finalize                                    # 固定边
react           ──(should_continue_react)──► react | finalize  # loop_stop_reason 为空则自环
finalize        → END
```

### 5.2 路由映射细节

- `route_by_intent`：`quiz→quiz_manager`，**`submit→error_analyzer`**（交卷直接进分析，不经 grading），`grade→grading`，`habit→habit`，`learn→knowledge_tutor`，**其余一切（含 chat）→ react**——答疑/闲聊走 ReAct 工具循环，这是「兜底即 react」的设计。
- `after_grading / after_analyzer`：产出为空就短路直出 finalize，失败不产生半截报告。
- `should_continue_react`：只看 `loop_stop_reason` 是否被置位——终止决策全部收敛在这一个字段上，trace/metrics 与图行为同源。

### 5.3 拓扑图

```
                    ┌───────────────┐
     START ────────►│ intent_router │
                    └───────┬───────┘
            ┌───────┬───────┼────────┬──────────┐
            ▼       ▼       ▼        ▼          ▼
      quiz_manager grading habit knowledge_tutor react◄─┐
            │       │       │        │          │       │ (自环，
            │       ▼       │        │          │       │  直至
            │  error_analyzer        │          │       │  loop_
            │       │       │        │          │       │  stop)
            │       ▼       │        │          │       │
            │  memory_manager        │          │       │
            ▼       ▼       ▼        ▼          ▼       │
      (submit 边绕过 grading 直达 error_analyzer)      │
                    ┌──────────────────┐               │
                    │     finalize     │◄──────────────┘
                    └────────┬─────────┘
                             ▼
                            END
```

---

## 6. 预防机制（全清单）

按「输入 → 路由 → 工具 → 循环 → 记忆 → 并发 → 鉴权」分层列出，全部为**代码层硬约束，不依赖 LLM 自觉**：

### 6.1 内容与隐私（`safety.py`，三层）

1. **输入审核 `check_input`**：违法/暴力/色情/自伤/代写作弊拦截；归一化对抗变体（全角→半角、去分隔符、谐音替换映射）；命中只记类别不记原文（防日志二次泄漏）；词表用词组避免误伤数学语境（「求根」「交点」不误杀）。
2. **PII 脱敏 `mask_pii`**：手机号/身份证/邮箱/QQ 微信/住址/学校班级 → 占位符；**排除数学上下文**（相邻是运算符/等号的长数字串视为算式不脱敏）；在进日志、trace、长期记忆前统一调用。
3. **输出审核 `sanitize_output`**：回复脱敏 + 拦截模型索取未成年人隐私的话术（「留下学校/电话/加微信」类越界）。

### 6.2 工具层（registry + toolguard）

| 机制 | 实现 | 防什么 |
|---|---|---|
| Pydantic 参数校验 | 非法参数拒绝 + 回注正确 schema | LLM 传错参产生半执行结果 |
| 依赖链 `requires` | 前置工具未成功调用即拦截 | 跳步骤（未 OCR 先批改） |
| 防重 `args_hash` | 同工具同参命中即 blocked | LLM 同参空转烧 token |
| 失败三分类 `classify_failure` | fatal(401/配额) > transient(超时/网络/429) > param | 该重试的重试、不该的立即停 |
| 指数退避重试 | 仅 transient、仅当前步骤、上限 2 次（0.4s×2ⁿ+抖动）；写类工具 `retryable=False` | 网络抖动放大 + 写入重复副作用 |
| 每工具熔断 | 滑窗 20 次、最少 5 次样本、失败率 ≥0.8 打开、30s 冷却半开重探 | 单工具故障雪崩拖垮全链 |
| 单工具超时 | 线程池隔离执行（16 workers），默认 30s、OCR 90s | 单工具卡死拖垮整条链 |
| 错误脱敏 `sanitize_error` | 绝对路径/长 token/邮箱打码 + 截断 200 字符 | 内部信息经 ToolResult/trace/prompt 泄漏 |
| 步骤台账 StepLedger | `math:task:{task_id}` 每步落 Redis/内存，TTL 1h，steps 有界 32 步 | 失败不可诊断、无法断点续跑 |
| 台账失败静默 | ledger_* 全 try/except | 可观测组件故障阻断主链路 |

### 6.3 ReAct 循环终止（react.py，四道闸）

1. **最大轮数**：`react_loop_count ≥ max_react_loops(8)` → `max_loop` 强制输出。
2. **信息增益**：本轮 Thought 向量与上轮余弦 ≥0.80（思维停滞）→ `information_gain` 立即收敛。
3. **重规划上限**：失败（**含 blocked 同参重复与 param**——重试无意义类同样计数）`tool_replan_count > 2` → `degraded` 降级出部分结果。
4. **fatal 直停**：鉴权/配额类不重试不重规划 → `fatal_error`，基于已获取观察作答。

配套：`fallback_tools`（search_knowledge 失败 → 强制换 search_user_memory，`react_forced_action` 用后即清）；`loop_stop_reason` 全部落 trace + metrics（循环终止原因分布可监控）。

### 6.4 记忆与状态有界

- `prune_state`：旧 `tool_call_history` 压缩为最小字段（防重语义不变）；observations 滑动窗口；已交卷会话整包 questions 释放（保留 records）。
- 分层记忆：实体锚永不释放、近期原文窗口 N 轮、滑出旧轮压缩一行要点、无实体闲聊轮整轮释放。
- 台账 steps ≤32、错误文本 ≤200 字符、checkpointer 只 carried 六键——三处防状态无限膨胀。

### 6.5 并发与鉴权

- 同用户 `MathAgent.invoke` 持 `RLock` 串行化（checkpointer/last_state 不可重入）；全局并发闸门在 `concurrency.py`。
- 身份以 `/api/session` 签发的 **HMAC token** 为准（`api/auth.py`），客户端自报 user_id 不可信；管理端点另有 admin 校验。
- OCR 上传：仅允许 `data/uploads/` 白名单扩展名。
- Redis 客户端构造期 **ping 探活**（socket 超时 2s）——懒连接不报错 ≠ 可达。
- Qdrant 用户记忆检索**强制注入 user_id**，缺失抛 `IsolationError`，绝无跨用户返回。

### 6.6 输出结构保护（LLM 红线）

报告标题/正确率/错因分布、讲解骨架（◆ 定义/要点/易错点/口诀）一律**代码层渲染**；LLM 只允许追加段落（「💡 老师建议」「◆ 老师讲讲」），不允许覆写结构——接 LLM Key 后结构化输出不会被改写。

---

## 7. 补充

### 7.1 意图路由 12 级优先级（`router._rule_route`，改路由必读）

```
空输入 → 学习模式分支（概念咨询→chat；非任务指令→learn）
→ 概念咨询 _P_CONCEPT（优先于作答判定）
→ 刷题中短输入作答（选项 A-D / 纯数字）
→ submit → grade → habit → learn（必须在 quiz 之前，否则"我想学函数"误判刷题）
→ quiz → 刷题中默认继续作答 → 兜底交 LLM / chat
```
坑位提示：`_P_CONCEPT` 里**不能**加「解析/提示」（那是 quiz_manager 单题反馈指令）；判分关键词须 `判\s*(?:一\s*下|下)?\s*分` 才覆盖口语。

### 7.2 API 端点（`api/server.py`）

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/session` | POST | 签发 HMAC token（身份唯一可信来源） |
| `/api/chat` | POST | 同步对话 |
| `/api/chat/stream` | GET | SSE 流式对话 |
| `/api/upload` | POST | OCR 上传（白名单扩展名 + 内容净化） |
| `/api/stats` | GET | 用户学习统计 |
| `/api/curriculum` | GET | 课程目录 |
| `/api/reset` | POST | 清该用户 attempts/quiz_sessions/agent_states + 内存 Agent |
| `/api/metrics` | GET | 工具成功率/意图分布/循环终止/**toolguard 计数**（retry/timeout/circuit/replan/fallback） |
| `/api/traces` + `/trace` | GET | 调用链明细 + P50/P95 耗时（已脱敏） |
| `/api/health` | GET | 健康检查 |

### 7.3 LLM 双模与降级矩阵

| 环节 | 有 Key | 无 Key（离线规则模式） |
|---|---|---|
| 意图路由 | 规则优先，未命中 LLM JSON 兜底 | 12 级正则全覆盖 |
| ReAct Thought/Action | LLM 生成 + JSON 决策 | 固定三步规则轨迹（检索→计算→组织答案） |
| 答案组织 | 300 字内辅导口吻 | 知识库检索结果 + 计算结果拼装 |
| 报告/讲解 | 骨架照渲染，LLM 只追加建议段 | 骨架照渲染，无追加段 |

关键判定：LLM 真正贡献了内容 =「拿到非空文本」，而非「LLM 可用」——失败路径返回 None 必须有规则兜底。

### 7.4 运维与边界声明

- **评测基线**（改动路由/判分/记忆/讲解/安全/toolguard 后必跑）：`eval_optimizations`(34/34)、`eval_conversation`(11/11)、`eval_memory`(21/21)、`eval_security`(16/16)、`eval_teaching`(19/19)、`eval_badcase`(49/49，含 47-49 toolguard)、`pytest`(26 passed+6 skipped)。⚠ 跑评测前先停 API 服务（Qdrant 本地模式单进程锁）；⚠ 接 LLM Key 后必须重跑全量（离线模式不暴露「LLM 覆写结构化输出」类问题）。
- **toolguard 进程内边界**：重试/熔断/台账为单 uvicorn 进程实现；多实例部署需迁移 Redis 分布式锁与分布式熔断（模块 docstring 已注明）。
- **配置热区**：`graph.max_react_loops`(8)、`graph.info_gain_threshold`(0.80)、`toolguard.max_replans`(2)、`toolguard.max_retries`(2)、`toolguard.circuit_*`、`toolguard.ledger_ttl_s`(3600) 均在 config.yaml，代码读 `cfg.get` 默认值兜底。
- **开发纪律**：同一文件多处修改严禁并行批量提交（历史上三次静默丢编辑），必须逐个改 + 逐个 grep 验证落盘。

---

*本文档基于 2026-09-16 源码快照撰写；对应代码：`src/math_agent`（state.py / builder.py / router.py / nodes.py / react.py / registry.py / toolguard.py / safety.py / compressor.py）。*
