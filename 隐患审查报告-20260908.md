# 智能助教 Agent 隐患审查报告

审查时间：2026-09-08 ｜ 范围：`src/math_agent` 全量 + `api/server.py` + 数据层
视角：企业级生产化（安全 / 并发 / 资源 / 正确性 / 运维）

## 总览

| 级别 | 数量 | 主题 |
|---|---|---|
| P0（必须修） | 3 | 越权访问、任意文件读写、上传无限制 |
| P1（上线前修） | 5 | 并发竞态、内存泄漏×2、SQLite 锁、CORS |
| P2（建议修） | 6 | reset 无效、session 碰撞、全表加载、评测污染生产库等 |

---

## P0 —— 安全与隐私（公网部署即事故）

### 1. 身份即自报，长期记忆/学习记录可被任意越权读取
`/api/chat`、`/api/stats`、`/api/upload` 的 `user_id` 全部由**客户端任意传入**，服务端无任何鉴权。
`user_long_term_memory` 的 `IsolationError` 隔离是按 user_id 字符串隔离的——但攻击者只要换一个
user_id 就能读、写**任意学生**的薄弱点、错题历史（`/api/stats?user_id=他人`直接返回答题记录）。
隔离机制本身是对的，缺的是"user_id 与真实身份的绑定"。
**建议**：接入登录态（JWT/Session），服务端从凭据解出 user_id，禁止信任请求参数；至少加一个共享密钥头。

### 2. 任意文件读取（OCR 工具）
`tools/ocr.py::ocr_document` 直接 `open(用户传入的 path)`，无目录白名单。
用户发「批改 C:/Users/lenovo/.env」→ OCR 把 `.env`（含 GLM_API_KEY）内容读出，
经 `grade_assignment` 回显到对话里。**密钥与任意本地文件可被外泄**。
**建议**：白名单限定在 `data/uploads/` 下（`Path.resolve()` 后校验前缀），拒绝其余路径。

### 3. 上传接口：路径遍历 + 无大小限制
`server.py::upload`：`dest = UPLOAD_DIR / f"{user_id}_{ts}_{file.filename}"`。
`filename`/`user_id` 含 `../` 或 `\..\` 时可写到 UPLOAD_DIR 之外（未 normalize 校验）；
`await file.read()` 一次读入内存，无大小上限 → 内存 DoS；未校验文件类型。
**建议**：`Path(...).name` 剥离目录成分 + resolve 后校验前缀 + 限流（如 10MB）+ 扩展名白名单。

---

## P1 —— 并发与资源（多用户/长时间运行必现）

### 4. 同一用户并发请求 = 数据竞态
FastAPI 同步端点跑在线程池；`_AGENTS[user_id]` 单例共享 `InMemorySaver` checkpointer、
`compressor`、`last_state`。同一学生开两个页面同时发消息：
- checkpointer 状态交叉覆盖（quiz_session 串题）；
- `invoke` 里 `last_state` 读取→回写是**丢失更新**（carried 键被并发轮互相冲掉）；
- `compressor.add_turn` 非加锁。
**建议**：按 user_id 加 `threading.Lock` 串行化 invoke；或队列化单用户请求。

### 5. `_AGENTS` 无淘汰 → 内存泄漏 + 放大攻击面
`server.py::_agent`：每个新 user_id 永久驻留一个 MathAgent（图+checkpointer+对话历史）。
恶意刷随机 user_id 即可打爆内存。
**建议**：LRU/TTL 淘汰（如 30 分钟未用即驱逐，驱逐时 compressor 已持久化到 ShortTermMemory，可安全重建）。

### 6. LangGraph InMemorySaver 检查点只增不减
InMemorySaver 保存**每个 superstep 的全部历史检查点**，从不淘汰。多轮长会话下，
即使 state 本身已做 prune，检查点历史仍线性增长（这正是"多路对话后变慢变重"的另一半来源）。
**建议**：长会话定期重建 MathAgent（丢弃 checkpointer）；或自研仅保留最新检查点的 saver。

### 7. SQLite 并发写：database is locked
`models.py` 用 `check_same_thread=False` 但默认连接池 + 无 `timeout`、未开 WAL。
线程池并发写 attempts 表时，SQLite 写锁冲突会直接抛错（答题记录静默丢失——
`quiz_manager_node` 里 `record_attempt` 是裸 try/except pass）。
**建议**：`connect_args={"timeout": 30}` + `PRAGMA journal_mode=WAL`；或生产切 MySQL（配置已留口）。

### 8. CORS 全开 + 无鉴权无限流
`allow_origins=["*"]` 且所有端点无认证、无速率限制。结合 P0-1，等于把全体学生数据公开。
**建议**：收敛 origin；至少对 chat/upload 加简单令牌。

---

## P2 —— 正确性与工程质量

9. **`MathAgent.reset()` 是假重置**：只清了 `last_state`，checkpointer 里的 quiz_session/
   tool_call_history 仍存活（下一轮 carried 为空但不携带 ≠ 清除）。用户"重新开始"会撞上旧会话。
   → reset 时应显式回注空值：`quiz_session={}, tool_call_history=[], short_term_memory={}`。

10. **session_id 秒级碰撞**：`s_{int(time.time())}` 只按 session_id 查重，两个用户同秒开刷，
    后者的 create_session 被跳过，attempt 统计错挂到前者会话。→ 加 user_id/uuid。

11. **`fetch_questions` 全表捞取**：`s.scalars(stmt).all()` 拉全板块再 Python 侧过滤洗牌。
    2009 条尚可，题库过万即成慢查询。→ `ORDER BY RANDOM() LIMIT n` 或预抽 id 池。

12. **评测脚本直写生产库**：`eval_*` 与真实服务共用 `data/math_agent.db` 和向量库，
    跑评测会污染真实答题记录。→ 评测用独立 DB/ collection 或 fixture 隔离。

13. **`chat_json` 贪婪正则**：`\{.*\}` 会吞跨多个 JSON 块的文本，LLM 输出里有两段 JSON 时解析错。→ 非贪婪 + 首块截取。

14. **`quiz_manager` 跳题文案与状态不符**：`index >= len` 后仍说"已跳到最后一道题"，且未把
    `finished` 置位，交卷提示依赖下一轮路由。属体验问题，顺手修。

## P3 —— 可观测性/运维

15. 日志全部 `print`，无级别/无结构化，多 worker 下不可检索 → 换 `logging` + JSON 格式。
16. `metrics` 是进程内计数器：uvicorn 多 worker 时各算各的，`/api/metrics` 只反映单 worker。→ 汇聚到 Redis 或按 worker 暴露。
17. ReAct 每轮 `embed_one(thought)` 走 CPU BGE，串行推理是单请求延迟大头；可加批处理/缓存。
18. 无健康探针分层（/api/health 已做轻量，好评）；无优雅停机处理 Qdrant close 的已知告警。

---

## 已经做对的部分（无需动）

- 判分先归一化到选项下标、记忆隔离抛 `IsolationError`、循环双重终止、工具防重——
  关键行为代码层硬约束的路线执行得很干净；
- LLM 不可用时全流程可跑（离线规则模式），降级路径完整；
- 压缩器对防重语义（args_hash/ok 保留）与实体锚的保护设计正确，`eval_memory` 21/21 覆盖到位。

## 修复优先级建议

1. **先堵 P0**：上传路径净化（10 行）→ OCR 白名单（10 行）→ user_id 鉴权（涉及前端，工作量中等）。
2. **再修 P1**：用户级 invoke 加锁（5 行）+ `_AGENTS` TTL 淘汰（20 行）+ SQLite WAL/timeout（2 行）。
3. P2 按顺手程度消化，其中 `reset()` 假重置建议尽快修（影响用户体验正确性）。
