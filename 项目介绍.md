# 初中数学辅导 Agent · 技术视角项目介绍

> **工程定位**：一个以 LangGraph 状态机为骨架的垂直领域辅导 Agent，核心工程命题是——**在 LLM 输出不可完全信赖的前提下，如何用代码层硬约束保证产品的关键行为确定性**。所有结构化产物（判分、报告、大纲）由代码渲染，LLM 仅做受控增强；全链路三级降级，离线亦可完整运行。

- 在线实例：https://math-tutor-agent.app.workbuddy.link/（单端口 HTTP 服务，公网沙箱内 torch + BGE 全量可用）
- 技术栈：Python 3.13 · LangGraph · FastAPI · GLM-4-flash/GLM-4V · Qdrant · BGE-small-zh-v1.5 · SQLite(WAL) · Redis 5 · SymPy · pytest · Docker/CI

---

## 1. 系统架构

```
Web 前端（KaTeX · SSE · localStorage 身份持久化 · 响应式）
    │  HTTPS（反向代理，公网单端口）
FastAPI（HMAC 鉴权 · 上传净化 · 限流 · metrics/traces）
    │
LangGraph 状态机（9 节点 ReAct 循环）
    │  意图路由 → 出题/判分/讲解/报告/习惯分析 → 工具调用
    │
┌───────┬──────────────┬─────────────┐
Qdrant 本地嵌入式   SQLite WAL     Redis（可选）
双 Collection 隔离  5 张持久化表   短期记忆，降级内存
```

**关键决策与权衡**：

| 决策 | 依据 |
|---|---|
| LangGraph 自研门面 + 手动状态回注，而非 SqliteSaver | 当前版本 checkpointer 不可用；自研落盘（QuizSession/AgentState 表）以零新依赖达成同等持久化语义，且跨轮字段裁剪完全可控 |
| Qdrant 本地嵌入式而非服务端 | 单机部署零运维；代价是单进程锁约束（评测前须停 API），已在启动文档显式声明 |
| SQLite WAL 而非 MySQL | 读多写少、单机场景；5 张表（questions/quiz_sessions/attempts/agent_states/trace_turns）承载题库与会话持久化，保留 SQLAlchemy 迁移路径 |
| Redis 仅作短期记忆缓存且可降级 | 会话态 TTL 86400s；连接失败自动回退内存字典，主流程零中断（工程上验证过 redis-py 8.x RESP3 与 Redis 5 服务端的握手不兼容问题，固定 4.x） |

## 2. 核心子系统

### 2.1 判分引擎（正确性关键路径）
- **数学等价性**：SymPy 化简比较，`2x+1` ≡ `1+2x`；选择题先做选项下标归一化再比较——修复过"答案是 A、用户答 B 判对"的判分漏洞（前缀识别正则不严格导致）。
- **LaTeX 归一化管线**：对题库全量扫描，100+ LaTeX 命令映射表（含组保留型命令单独规则），残留扫描清零；清洗规则带回归断言防退化。
- **单题反馈硬约束**：回复必含"回答错误"、单题内禁出分析报告——在代码层断言，不依赖提示词遵循。

### 2.2 意图路由（正则优先级状态机）
空输入 → 学习模式分支 → 概念咨询 → 刷题中短输入作答 → 交卷 → 判分 → 习惯报告 → **学习模式（先于刷题）** → 刷题 → 兜底。路由顺序本身是回归对象：`_P_LEARN` 误排到 `_P_QUIZ` 之后曾导致"我想学函数"被误判为刷题，已由 34 条路由回归覆盖。

### 2.3 记忆系统（三层）
- **短期**：Redis（TTL 随会话）/内存双后端，统一接口；
- **会话持久化**：每轮 invoke 后落盘 QuizSession/AgentState，重启恢复题目与进度（已验证）；跨轮状态采用"上一轮 state 裁剪 + 显式回注 payload"策略，规避 LangGraph payload 与 checkpointer 状态不一致的陷阱；
- **长期**：Qdrant 按 user_id 强制隔离过滤（缺失直接抛 `IsolationError` 而非静默跨用户检索）；上下文预算 1600 字符，压缩器按 turn 淘汰/压缩，实体锚上限 10，原始轮次上限 60。

### 2.4 教学引擎
38 个知识点讲解骨架（定义/要点/易错点/口诀）由代码渲染；LLM 仅追加"老师讲讲"口语化段——曾因放开改写导致大纲丢失的回归，催生了「**LLM 输出是否进入结构化区域**」的硬约束清单（现 10 条，全部配有 badcase 回归）。配套分层提示三级模型（方向 → 关键步骤 → 揭晓），保证学生始终处于最近发展区。

## 3. 数据工程

- **ETL 管线**：TAL-SCQ5K 5000 条原始 → 清洗去重 542 → 参数化模板补几何缺口 373 → 派生 **2034 题**（函数 519 / 方程 572 / 几何 559 / 综合 384；选择 927 / 填空 923 / 应用 184），四大板块均 ≥500。
- **候选源评估留痕**：cn-k12 经采样 99.8% 英文且偏高中，判定不可用并记录，避免后续重复试错。
- **重建幂等**：build 脚本先 reset 再重建，question_id 取自数据本身而非列表下标，杜绝脏数据残留。

## 4. 可靠性设计

| 依赖失效 | 行为 |
|---|---|
| LLM API 不可用 | 规则模板生成全量功能产物，`llm:false` 显式可见 |
| torch/BGE 缺失或冷加载 | hashing 向量兜底；启动预热线程加载 BGE，`embedder_ready` 暴露状态 |
| Redis 不可达 | 内存字典兜底（短周期场景语义等价） |
| 工具异常 | `log.exception` + `metrics.record_quiz_error()` 计数，拒绝静默吞异常（曾修复 3 处 `except:pass`） |

## 5. 安全模型

- **认证**：`/api/session` 签发 HMAC-SHA256 token（30 天 TTL），受保护接口身份从 token 解出，客户端自报 user_id 不被信任；公网强制 `require_token=true`。
- **租户隔离**：长期记忆检索强制注入 user_id 过滤，代码层抛异常而非静默放行。
- **输入面**：OCR 白名单扩展名 + 大小限制 + 文件名净化防路径逃逸；PII（手机号/身份证）入库前脱敏。
- **审计**：trace 落库（上限 2000 行滚动），输入/输出脱敏后持久化。

## 6. 质量保障体系

**七套验收基线 + 单元冒烟**，任何涉及判分/路由/记忆/讲解/安全的改动必须全绿：

| 套件 | 基线 | 防护对象 |
|---|---|---|
| 判分回归 | 34/34 | 等价性、选项归一化、LaTeX |
| 意图路由 | 11/11 | 优先级次序、边界输入 |
| 记忆优化 | 21/21 | 压缩/释放/state 有界 |
| 安全 | 16/16 | 鉴权/上传/并发/WAL |
| 教学闭环 | 19/19 | 讲解骨架、硬约束 |
| 历史踩坑回归 | 49/49 | 全部已知 bug 的反例（含工具链失败治理 47-49） |
| pytest 冒烟 | 7 passed | 端到端冒烟 |

工程纪律：eval 脚本内置真断言（pytest 严格模式下失败即抛，消灭"打印 PASS 无断言"的假绿）；评测自身缺陷同样纳入治理（曾揪出一条语义永假的断言）。CI（GitHub Actions）跑 pytest + 与本地数据无关的评测门禁。

## 7. 性能与可观测性

- `/api/metrics`：工具错误率、循环终止分布、意图分布、会话压缩释放量；
- `/api/traces`：调用链明细 + P50/P95 延迟，脱敏后 SQLite 持久化；
- Agent 池：LRU + TTL（1800s/100 容量）控制内存，淘汰安全（状态已持久化）；
- BGE 冷加载（数十秒）由预热线程吸收，首个请求不阻塞主线程。

## 8. 量化汇总

| 维度 | 数值 |
|---|---|
| 题库规模 | 2034 题（四大板块均 ≥519，题型三分覆盖） |
| 教学覆盖 | 6 模块 / 38 知识点 |
| 工具 | 11 个（出题/判分/讲解/报告/记忆/检索等） |
| 评测基线 | 7 套 / 157 项断言全绿 |
| 硬约束清单 | 10 条（均源自真实踩坑并配回归） |
| 部署形态 | 本机 / Docker / 公网单端口三种，同一份代码 |

## 9. 边界与演进

已知取舍：单机架构（Qdrant 进程内、SQLite）、SSE 当前为整段返回非逐 token 流式、家长/老师视图导出未做。演进方向按优先级：SSE 真流式 + LLM 重试预算 → 报告导出 → 题库扩充（几何证明/图像交互题）。

---

*配套：《技术文档》（数据流向五链路/硬约束清单全文）、《启动流程》（运维手册）、《双视角不足清单》（质量治理实录，39 项不足的闭环记录）*
