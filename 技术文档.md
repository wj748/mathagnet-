# 智能助教（初中数学辅导 Agent）技术文档

> 版本：v2.0 ｜ 更新日期：2026-09-11 ｜ 运行环境：Windows / Python 3.13（可容器化部署）

---

## 1. 项目概述

基于 **LangGraph** 构建的初中数学辅导 Agent，面向初中生提供两种核心模式：

- **刷题模式**：按板块/题型出题 → 作答 → 判分 → 分层提示 → 错因分析 → 错题报告 → 学习习惯追踪
- **学习模式（知识点讲解 Agent）**：选板块/知识点 → 讲解 → 例题 → 小测 → 判分讲评 → 下一个知识点

两大设计原则（全项目的第一性原理）：

1. **关键行为在代码层硬约束，不依赖 LLM 自觉**——判分、路由、结构化输出（报告/讲解骨架）、记忆隔离、安全审核全部由代码保证；LLM 只做润色与补充，**只允许追加、不允许覆写骨架**。
2. **无 API Key 全流程可用**（离线规则模式）——接入智谱 GLM（或任意 OpenAI 兼容端点）后自动升级为 LLM 增强模式，结构与验收基线不变。

**当前规模**：6 大教学板块 38 个知识点 ｜ 题库 **2034 题**（函数 519 / 二元一次方程 572 / 几何图像 559 / 综合 384；选择题 927 / 填空题 923 / 应用题 184）｜ 11 个工具 ｜ 9 个图节点 ｜ 7 套评测全绿。

---

## 2. 应用框架

### 2.1 分层架构

```
┌─────────────────────────── Web 前端（web/index.html）───────────────────────────┐
│ 刷题/学习双模式 · KaTeX 公式渲染 · 数学符号快捷键 · 作答面板 · FAB 悬浮上传       │
│ localStorage 身份持久化 · 等待态呼吸点 · 调试面板折叠 · 「重置我的学习数据」      │
└────────────────────────────────────┬───────────────────────────────────────────┘
                                     │ HTTP / SSE
┌──────────────────────────── FastAPI 服务（api/server.py）───────────────────────┐
│ /api/session(HMAC 鉴权) · /api/chat · /api/chat/stream(SSE) · /api/upload(OCR)   │
│ /api/stats · /api/curriculum · /api/reset · /api/metrics · /api/traces · /trace  │
└────────────────────────────────────┬───────────────────────────────────────────┘
                                     │
┌────────────────────────── LangGraph StateGraph（graph/）────────────────────────┐
│ intent_router ──┬─ quiz_manager（出题/交卷/分层提示/解析）                       │
│   （12 级规则    ├─ grading（判分）→ error_analyzer（错因分析）                  │
│    优先级路由）  ├─ memory_manager（记忆读写/压缩）                              │
│                 ├─ habit（学习习惯报告）                                         │
│                 ├─ knowledge_tutor（学习模式：讲→练→测）                         │
│                 └─ react（ReAct 工具循环，含循环终止保护）                       │
└──────┬──────────────┬──────────────┬───────────────┬───────────────────────────┘
       │              │              │               │
  tools/（11 工具）  db/（SQLite）  vectorstore（Qdrant）  memory/（短期+压缩）
                                       ↑
                             embeddings（BGE-small-zh-v1.5，无 torch 回退 hashing）
                                       ↑
                             LLM（GLM-4-flash 文本 / GLM-4V-flash 视觉，可选）
```

### 2.2 图节点与流转（graph/builder.py）

| 节点 | 职责 |
|---|---|
| `intent_router` | 12 级规则优先级路由（空输入 → 学习模式内分支 → 概念咨询 → 刷题中短输入作答 → submit → grade → habit → **learn（先于 quiz）** → 兜底）；LLM 路由仅在规则低置信时启用 |
| `quiz_manager` | 出题、交卷、**分层提示**（level1 方向 → level2 解析前两句 → level3 揭晓答案）、单题解析（analysis 空时兜底通用步骤模板）、快捷指令（板块+题型+数量，单次上限 20） |
| `grading` | 代码层判分（选择题**先映射选项下标再比较**），触发错因分析 |
| `error_analyzer` | 错题归因（概念/计算/审题），LLM 只润色不改结论 |
| `memory_manager` | 用户长期记忆读写（Qdrant 强制 user_id 隔离）、短期记忆压缩 |
| `habit` | 学习习惯报告——正文代码渲染，LLM 只追加「老师建议」段 |
| `knowledge_tutor` | 学习模式状态机（teach → quiz → graded → next），小测答错引导重试 |
| `react` | 通用工具循环（ReAct），最大步数/重复调用熔断，终止原因入 metrics |
| `finalize` | 汇合收尾，后 END |

### 2.3 状态管理与持久化

- 跨轮状态持久化在 checkpointer；每轮 invoke 传入新 dict，跨轮字段（`tool_call_history`、`learning_session` 等）须从上一轮 `last_state` 取出、裁剪后**显式回注 payload**（`compressor.prune_state`），保证状态有界。
- **自研落盘层（2026-09-10 新增）**：SqliteSaver 当前版本不可用，改为 SQLite 表持久化——`quiz_sessions`（进行中刷题会话）、`agent_states`（跨轮状态白名单键）、`trace_turns`（调用链，上限 2000 行滚动清理）。进程重启自动恢复题目与进度，`reset()` 同步清表。

---

## 3. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| Agent 编排 | **LangGraph** StateGraph | 9 节点条件路由；InMemorySaver + 自研 SQLite 落盘 |
| 服务框架 | **FastAPI** + Uvicorn | REST + SSE 流式；HOST/PORT 环境变量可配（容器用） |
| LLM | 智谱 **GLM-4-flash**（文本）/ **GLM-4V-flash**（视觉 OCR） | OpenAI 兼容端点，可换 DeepSeek/通义/本地 vLLM；无 Key 自动降级规则模式 |
| 向量库 | **Qdrant** 本地嵌入式 | 双 Collection 隔离，零 Docker 依赖；检索内置 score_threshold（0.10） |
| Embedding | **BGE-small-zh-v1.5** 本地权重（ModelScope） | 无 torch 时自动回退 hashing 向量（链路不断） |
| 关系库 | **SQLite**（SQLAlchemy 2.x，WAL 模式） | 5 张表：questions / quiz_sessions / attempts / agent_states / trace_turns |
| 短期记忆 | Redis（可选）或内存兜底 | docker-compose 起 Redis 7（AOF+LRU+健康检查） |
| 公式渲染 | **KaTeX 0.16.11** 本地化 | 离线可用，经 `/static` 挂载 |
| 身份安全 | HMAC token（自实现） | `/api/session` 签发，客户端自报 user_id 不可信 |
| 测试 | pytest + 7 套自研评测 | eval 脚本内置**真断言门禁**（PYTEST_CURRENT_TEST 触发） |
| 部署 | Dockerfile + docker-compose + GitHub Actions CI | requirements.lock.txt 锁定 108 项依赖版本 |

**语言与运行时**：Python 3.13；前端原生 HTML/JS/CSS（单文件，零构建）；CI 辅助脚本 Node.js。

---

## 4. 实施步骤

### 4.1 分阶段落地历程（实现方案 Phase 0~5）

| 阶段 | 内容 | 产出 |
|---|---|---|
| **Phase 0 骨架** | 项目结构、config.yaml+.env 配置、依赖约定 | 可导入的 `math_agent` 包 |
| **Phase 1 题库与工具层** | TAL-SCQ5K ETL（四级镜像降级下载）、LaTeX 清洗、39 个参数化模板补缺、SXT001 接入、build_question_bank | 2034 题 SQLite + Qdrant |
| **Phase 2 最小闭环** | StateGraph：router → quiz_manager → grading → finalize；代码层判分 | 无 LLM 可跑通刷题 |
| **Phase 3 四项优化** | 记忆压缩（compressor）、ReAct 循环熔断、检索阈值、状态瘦身 | eval_optimizations 34/34 |
| **Phase 4/5 服务化与验收** | FastAPI + SSE、Web 前端（KaTeX 渲染管线）、7 套评测体系 | 端到端可用的产品 |
| **安全加固** | OCR 白名单、路径遍历净化、HMAC 鉴权、PII 脱敏、内容审核、Agent 池 TTL、WAL | eval_security 16/16 |
| **双视角修复（2026-09-10）** | 评测真断言门禁、分层提示、会话/轨迹落盘、板块单一真源（topics.py）、find_point_index 最长匹配、FAB/重置/上传容错、Dockerfile+CI | 清单 1-12/14 项全落地 |

### 4.2 日常构建与部署步骤

```bash
# 1) 依赖（torch 必须单独装，不能写进 requirements.txt 行内）
pip install -r requirements.lock.txt          # 或 requirements.txt（宽松版）
pip install --index-url https://download.pytorch.org/whl/cpu torch

# 2) LLM 配置（可选）：.env 写入 GLM_API_KEY / GLM_BASE_URL / GLM_MODEL
# 3) Redis（可选）：docker compose up -d redis

# 4) 建库（改动题库数据后按此顺序）
python scripts/gen_geometry_questions.py --per-template 10   # → data/processed/synth_questions.jsonl
python scripts/build_question_bank.py --with-vector          # 合并 → SQLite + Qdrant（自动 reset 防脏数据）

# 5) 启动（本机）
PYTHONPATH="src" python -m math_agent.api.server             # http://127.0.0.1:8787
# 容器：docker build -t math-agent . && docker run -p 8787:8787 --env-file .env -v $(pwd)/data:/app/data math-agent

# 6) 改动路由/判分/记忆/讲解/安全后必跑全量评测（先停服务——Qdrant 单进程锁）
python scripts/eval_optimizations.py && python scripts/eval_conversation.py && ...
```

---

## 5. 数据流向

### 5.1 刷题主链路（出题 → 作答 → 判分 → 报告）

```
用户输入「我要刷函数的选择题」
  → intent_router（规则路由 → quiz, 0.90+置信）
  → quiz_manager：parse（板块/题型/数量，MAX_COUNT=20）
      → topics.to_bank_topic() 板块映射（单一真源）
      → fetch_quiz 工具 → repository.fetch_questions（SQLite 随机抽样 + 去重）
      → 会话写入 quiz_sessions 表（questions JSON / index / records）
  → 用户作答 → grading：
      选择题 → resolve_option 选项下标映射 → answers_equal（单位剥离/多答案/数值等价）
      → record_attempt 写 attempts 表 → 错误时 error_analyzer 归因
      → 答错回复「回答错误 + 分层提示」（不出报告，硬约束）
  → 「交卷」→ analyze_errors / extract_habits（代码渲染报告正文）
      → llm_polish_report 仅追加「💡 老师建议」段
      → 错题/习惯写入 user_long_term_memory（Qdrant，强制 user_id）
```

### 5.2 学习模式链路

```
「我想学函数」 → learn 路由（优先级在 quiz 之前）→ knowledge_tutor
  → teaching.CURRICULUM（6 板块 38 知识点）→ find_point_index（序号 → 最长名匹配 → 片段匹配）
  → build_lecture：rule_lecture 代码骨架（◆定义/要点/易错点/口诀）+ llm_lecture 仅插「◆老师讲讲」
  → pick_examples：to_bank_topic 映射 → SQLite 例题（选择题优先）
  → 小测：答对继续 / 答错「再想想+提示」/「看答案」揭晓
  → 每轮状态存 learning_session（跨轮回注 + quiz_sessions 落盘）
```

### 5.3 记忆与上下文流

```
每轮 invoke：
  上轮 last_state → compressor.prune_state 裁剪（hist≤24 / obs 滑窗≤8 / state 有界）
  → 显式回注 payload → LangGraph 执行
  → 短期记忆：Redis（可选）或内存，TTL 管理
  → 长期记忆：search_user_memory 强制 user_id 过滤（缺省抛 IsolationError）
  → digest 注入 prompt（预算 1600 chars，实体保留）
  → 会话快照 save_agent_state → agent_states 表（单用户上限 200KB）
```

### 5.4 OCR 批改链路

```
前端选文件（accept 白名单 + 大小预检）
  → POST /api/upload（文件名净化防路径遍历 + 10MB 上限）
  → 写 data/uploads/（uid 前缀 + 时间戳 + 随机后缀）
  → ocr 工具：GLM-4V 视觉识别（无 Key 回退文本层）→ need_confirm 置信确认
  → agent.invoke("批改 <path>") → 判分/讲解 → 回复
  → finally：上传文件即删（OCR 文本已进状态），防磁盘增长
```

### 5.5 存储层一览

| 存储 | 内容 | 写入方 | 特性 |
|---|---|---|---|
| SQLite `questions` | 2034 题题库 | build_question_bank（重建式） | topic/type 索引 |
| SQLite `attempts` | 每次作答记录 | grading 节点 | user_id/topic 索引 |
| SQLite `quiz_sessions` | 进行中刷题会话 | quiz_manager | 重启恢复 |
| SQLite `agent_states` | 跨轮状态快照 | builder.invoke 末尾 | 单用户 200KB 上限 |
| SQLite `trace_turns` | 调用链 | trace.add() | 2000 行滚动清理 |
| Qdrant `knowledge_base` | 题库向量（题=完整 Chunk） | 建库脚本 | 只读检索 + 阈值过滤 |
| Qdrant `user_long_term_memory` | 错题习惯/历史报告 | memory_manager | **强制 user_id 隔离** |
| Redis / 内存 | 短期对话记忆 | memory_manager | Redis 失败自动降级 |

---

## 6. 项目亮点

1. **「代码层硬约束」纪律贯穿全项目**——判分、路由、报告/讲解骨架、记忆隔离、安全审核全由代码保证；LLM 只追加不覆写。无 Key 离线全流程可用，接 Key 结构与验收基线不变。
2. **7 套评测全绿且是真门禁**——6 套 eval（判分 34 / 路由 11 / 记忆 21 / 安全 16 / 教学 19 / badcase 49）+ pytest；eval 的 `check()` 内置 `AssertionError`（PYTEST_CURRENT_TEST 触发），CI 可直接启用，杜绝"假绿"。
3. **板块命名单一真源**——`src/math_agent/topics.py` 统一教学板块 ↔ 题库板块映射（`to_bank_topic`），消灭了三套映射漂移（曾出现同一边缘板块两处映射矛盾）；题量上限 MAX_COUNT=20 单点定义。
4. **分层提示教学设计**——答错不再只判对错：level1 方向提示 → level2 解析前两句 → level3 揭晓答案；解析缺失时兜底通用解题步骤模板。同时保留「单题不出报告」硬约束。
5. **完整可观测性**——自研 trace 面板（调用链明细 + P50/P95 耗时 + 循环终止分布 + 工具错误率，输入 PII 脱敏）+ 进程内 metrics（意图分布/conversation 压缩释放），并落 SQLite 重启可查。
6. **安全内建**——HMAC token 身份、OCR 目录白名单、路径遍历净化、上传大小上限、暴力/自伤/违法/代写审核（含 12355 热线引导）、PII 双向脱敏、向量记忆强制 user_id 隔离（`IsolationError` 兜底）。
7. **渐进式降级链**——LLM 缺失→规则模板；torch 缺失→hashing 向量；Redis 缺失→内存兜底；解析缺失→通用步骤模板。每一层退化都不断链路。
8. **数据质量可验证**——ETL 四级镜像降级下载、LaTeX 全库残留扫描（badcase 内置，残留=0）、`question_id` 来自数据本身防重建脏数据、干扰项手算验证。
9. **产品级前端细节**——KaTeX 本地化离线渲染（纯文本记法→LaTeX 管线）、26 个数学符号快捷键、localStorage 身份持久化、上传失败明确报错（含 iPhone HEIC 引导）、FAB 悬浮上传、家长/隐私说明入口、一键重置学习数据。
10. **工程化完备**——Dockerfile（CPU torch 专用索引 + HEALTHCHECK）、依赖锁 108 项、GitHub Actions CI、docker-compose（Redis + app profile）、`.env.example`。

---

## 7. 目录结构

```
智能助教/
├─ src/math_agent/
│  ├─ api/            server.py(FastAPI) · auth.py(HMAC token)
│  ├─ graph/          builder.py · router.py · nodes.py · knowledge_tutor.py · react.py
│  ├─ tools/          registry.py · quiz.py · grading.py · calc.py · report.py
│  │                  memory_tools.py · teaching.py · ocr.py（GLM-4V + 白名单）
│  ├─ memory/         short_term.py · compressor.py（状态裁剪/压缩）
│  ├─ db/             models.py(SQLAlchemy 5 表) · repository.py
│  ├─ topics.py       板块命名单一真源（BANK_TOPICS / TEACHING_TO_BANK / to_bank_topic）
│  ├─ config.py       config.yaml + .env（内置 _load_dotenv）
│  ├─ llm.py          OpenAI 兼容 client（文本 / 视觉双通道）
│  ├─ embeddings.py   BGE-small-zh-v1.5，无 torch 回退 hashing
│  ├─ vectorstore.py  Qdrant 双 Collection，检索内置阈值
│  ├─ teaching.py     CURRICULUM 大纲 + 讲解生成 + find_point_index + 例题挑选
│  ├─ analysis.py     错因分析 / 报告生成（LLM 只补建议段）
│  ├─ safety.py       内容审核 + PII 脱敏
│  ├─ trace.py        调用链缓冲 + SQLite 落盘（TraceTurn）
│  └─ metrics.py      Counter 指标（意图/循环终止/工具错误/quiz_errors）
├─ scripts/           ETL · 评测（6 套）· run_cli · 前端检测
├─ web/               index.html · trace.html · katex/（本地化）
├─ tests/             pytest 冒烟 + eval 门禁桥接
├─ data/              qdrant/ · math_agent.db · processed/ · raw/ · uploads/
├─ models/            BGE-small-zh-v1.5 本地权重
├─ Dockerfile · docker-compose.yml · .github/workflows/ci.yml
├─ requirements.txt（宽松）· requirements.lock.txt（锁定 108 项）
├─ config.yaml · .env.example · pytest.ini
└─ 技术文档.md · 实现方案.md · 双视角不足清单-20260910.md · 隐患审查报告-*.md
```

---

## 8. API 接口（api/server.py）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/session` | 签发 HMAC token（身份凭证，自报 user_id 不可信） |
| POST | `/api/chat` | 同步对话（JSON） |
| GET | `/api/chat/stream` | SSE 流式对话（trace 事件 + 逐字 delta） |
| POST | `/api/upload` | 图片/PDF 上传 → OCR 批改（白名单扩展名 + 净化 + 大小上限） |
| GET | `/api/stats` | 题库统计 + 用户做题数/正确率 |
| GET | `/api/curriculum` | 知识点目录（6 板块 38 知识点） |
| POST | `/api/reset` | 清空该用户学习数据（attempts/sessions/states + 内存 Agent 重建） |
| GET | `/api/metrics` | 运行指标（require_token=true 时需鉴权） |
| GET | `/api/traces` | 调用链明细 + 聚合统计 |
| GET | `/api/health` | 健康检查（llm / embedder 状态，不触发模型加载） |
| GET | `/`、`/trace` | 主界面、trace 面板 |

---

## 9. 安全设计（safety.py + api/auth.py）

| 层 | 机制 |
|---|---|
| 身份 | `/api/session` 签发 HMAC token；公网部署须将 `security.require_token` 置 `true` |
| 输入审核 | 暴力/自伤/违法/代写作弊拦截（含 12355 热线引导），纯规则离线生效，数学题零误伤 |
| 隐私 | `mask_pii`：手机号/身份证/邮箱/住址/学校班级；trace 存储前先脱敏 |
| 输出审核 | 拦截"留个微信"类索取隐私话术 |
| OCR | 仅允许 `data/uploads/` 白名单扩展名；文件名净化防路径遍历；用完即删 |
| 记忆 | 向量检索强制 user_id 隔离（`IsolationError`） |
| 静默失败治理 | 关键 except 全部 `log.exception` + metrics 计数（quiz_errors），不再吞异常 |

---

## 9.5 多用户并发治理（concurrency.py，2026-09-15）

全部进程内硬约束，配置在 `config.yaml` 的 `concurrency:` 段；拒绝响应统一为 429/503 + 初中生友好文案。

| 机制 | 实现 | 配置项（默认） |
|---|---|---|
| 同用户任务互斥 | `UserGate` 非阻塞抢占锁，第二条智能体请求立即 429「上一条还在处理中」 | — |
| 用户级 QPS | 1 秒滑动窗口，到达即计数（含被拒），超限 429 | `per_user_qps`（3） |
| 全局并发预算 | 信号量限制同时在跑的智能体任务数，保护下游 LLM API 配额；满时排队 | `max_agent_tasks`（8）、`queue_wait_s`（15） |
| 熔断防雪崩 | 在途任务达硬上限立即 503；排队超时降级 503 | `max_inflight`（32） |
| 请求幂等 | `X-Request-Id`/`request_id` → TTL+LRU 缓存，重放返回首次结果（`idempotent_replay: true`），trace 不入缓存 | `idempotency_ttl_s`（300）、`idempotency_max`（500） |
| 记忆层锁/版本 | `InMemoryStore` 全操作 RLock；compressor 会话版本戳（`version` 单调递增，可校验乱序写） | — |
| 可观测 | `/api/metrics` 新增 `concurrency` 段（admitted/busy/qps/overload/queue_timeout/replay）与 `breaker` 段（active/上限/累计拒绝） | — |

适用边界：以上为**进程内锁**，对应单 uvicorn 进程部署（现状）；多进程/多实例需迁移到 Redis 分布式锁（SET NX+TTL），已在模块 docstring 声明。upload 端点的阻塞推理已移入线程池（`anyio.to_thread`），不再阻塞事件循环。

---

## 9.6 工具链失败治理（toolguard.py，2026-09-15）

多工具链式调用中途失败的统一治理，全部代码层硬约束，配置在 `config.yaml` 的 `toolguard:` 段。

| 机制 | 实现 | 配置项（默认） |
|---|---|---|
| 失败分类 | `classify_failure`：transient（网络/超时/临时限流）/ param（参数业务错误）/ fatal（鉴权配额），fatal 优先判定 | — |
| 分层重试 | 仅 transient 指数退避重试（base×2^i+抖动），仅重试当前步骤；写类工具 `retryable=False` 不自动重试（幂等保护：`update_long_term_memory`） | `max_retries`（2）、`backoff_base_s`（0.4） |
| 单工具超时 | 线程池隔离执行，单工具卡死不拖垮链；`ocr_document` 单独放宽 90s | `default_timeout_s`（30） |
| 每工具熔断 | 滑窗失败率 ≥0.8（≥5 次）打开，冷却 30s 后半开探测；打开期间快速拒绝（blocked 标记） | `circuit_window/min_volume/fail_rate/cooldown_s` |
| Agent 重规划 | 失败信息脱敏后随 observation 回注 LLM（可修正参数/换工具）；**blocked（同参重复/依赖链拦截）与参数错误同类计入重规划上限**，避免 LLM 同参空转；规则层兜底：`fallback_tools` 配置同能力备选（search_knowledge→search_user_memory）强制换用；重规划上限 2 轮防死循环 | `max_replans`（2）、`fallback_tools` |
| 降级兜底 | fatal/重规划耗尽 → 停止链路，`_compose_answer` 基于已获取的部分结果作答 + 明确降级提示（`loop_stop_reason: fatal_error / degraded`） | — |
| 状态持久化 | `StepLedger`：task_id/step_id/tool/args_hash/状态/结果摘要（≤200 字）按步落 Redis/内存（TTL 1h）；`ledger_completed_map` 提取已成功步骤供断点续跑复用 | `ledger_ttl_s`（3600） |
| 错误脱敏 | `sanitize_error`：绝对路径/长 token/邮箱打码、限长 200，所有执行类错误进入 ToolResult/trace/prompt 前必经 | — |
| 可观测 | `/api/metrics` 新增 `toolguard` 段：retry/timeout/circuit_open/circuit_reject/replan/fallback 计数 + 每工具最终失败次数 | — |

任务绑定：每次进入 ReAct 链生成 `task_id`（state 字段），每步 `step_id = {task_id}-s{n}-{tool}`；trace 增加 `react[n]tool=xxx:ok/fail` 行。幂等由三层共同保证：registry args_hash 防重（同参重放拒绝）、写类工具不自动重试、台账按 args_hash 记录可复用步骤。

---

## 10. 验收与测试（改动后必跑）

| 命令 | 覆盖 | 基线 |
|---|---|---|
| `scripts/eval_optimizations.py` | 四项优化 + 判分准确性回归 | 34/34 |
| `scripts/eval_conversation.py` | 意图路由 + 学习模式路由 + 行为约束 | 11/11 |
| `scripts/eval_memory.py` | 多轮压缩/释放/state 有界 | 21/21 |
| `scripts/eval_security.py` | OCR 白名单/鉴权/上传净化/并发/WAL | 16/16 |
| `scripts/eval_teaching.py` | 知识大纲/离线讲解/学习闭环/硬约束 | 19/19 |
| `scripts/eval_badcase.py` | 历史踩坑回归（含板块真源 43-46、toolguard 47-49、LaTeX 全库扫描） | 49/49 |
| `pytest tests/ -q` | 冒烟 + 并发/工具链治理单测（+ eval 门禁桥接，默认 skip，CI 启用） | 26 passed |

> ⚠️ 跑评测前必须**先停 API 服务**——Qdrant 本地模式有单进程锁。
> ⚠️ 接入 LLM Key 后必须重跑全量——离线模式不暴露「LLM 覆写结构化输出」类问题。

---

## 11. 代码层硬约束清单（违反即回归）

1. 判分：选择题**先映射到选项下标再比较**，不允许文本宽松比较
2. 结构化输出（习惯报告/错题分析/讲解骨架）由**代码渲染**，LLM 只追加建议段（判定 LLM 真贡献看「是否拿到文本」，失败返回 None）
3. 记忆检索强制 user_id；评测探针必须在 finally 里删除
4. 模板题排版用 `_coef/_kx/_lin`，禁止 f-string 手拼（防 `-1x`）；干扰项必须手算验证不恒等
5. LaTeX 清洗走映射表 + `(?![a-zA-Z])` 边界；验证方式为全库残留扫描（badcase 内置）
6. 跨轮状态修改必须"从 last_state 取出→裁剪→显式回注"，只改 payload 无效
7. 同一文件多处修改**逐个改+逐个验证**，禁止并行批量 Edit
8. metrics 新增指标必须实现 record_* 并进 `__all__`，警惕 try/except 静默吞错
9. 板块/题量上限一律引用 `topics.py`（`to_bank_topic` / `MAX_COUNT=20`），禁止散落映射
10. eval 断言必须语义可满足（防"绝对目录 in 纯文件名 parents"类永假式假绿）

---

## 12. 后续方向

- SSE 真流式（LLM token 级）+ 断连检测；LLM 重试与整轮超时预算
- 题库补充：统计与概率、锐角三角函数当前回退综合题库，可定向补题
- 家长/老师视图：报告导出（趋势 + 薄弱点 TOP）、轻量账号绑定
- 无障碍增强：对错双重标识强化
- 公网部署：`security.require_token=true` + HTTPS + 反向代理
