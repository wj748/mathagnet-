# 初中数学辅导 Agent —— 全流程梳理

> 更新：2026-09-08 ｜ 对应《实现方案.md》Phase 0~5 全部完成 + 记忆压缩优化 + 安全加固

---

## 一、项目定位与设计原则

LangGraph 初中数学辅导 Agent：刷题 / 批改作业 / 错题分析 / 学习习惯追踪 / 概念答疑。

**三条硬原则**（贯穿全部代码）：

1. **关键行为在代码层硬约束，不依赖 LLM 自觉**——判分归一化、记忆隔离、循环终止、工具防重、OCR 白名单、身份鉴权全部是代码强制执行。
2. **无 API Key 也能跑通全流程**——离线规则模式（模板文案），配 `GLM_API_KEY` 后自动切 LLM 润色。
3. **有界性**——state、对话记忆、Agent 池都有硬上限，多轮/多用户不会无限膨胀。

---

## 二、目录结构速览

```
智能助教/
├── config.yaml               # 全部配置（llm/embedding/vectorstore/db/security/memory…）
├── data/
│   ├── raw/                  # TAL-SCQ5K 原始 jsonl
│   ├── processed/            # 模板生成的 synth_questions.jsonl
│   ├── math_agent.db         # SQLite 业务库（题库/会话/答题记录，WAL 模式）
│   ├── qdrant/               # 向量库（双 Collection）
│   └── uploads/              # 上传白名单目录（OCR 只认这里）
├── models/bge-small-zh-v1.5  # 本地 BGE 嵌入模型
├── scripts/                  # ETL / 模板生成 / 五套验收评测
├── src/math_agent/
│   ├── api/                  # server.py(FastAPI+SSE) + auth.py(HMAC 鉴权)
│   ├── db/                   # models.py + repository.py
│   ├── graph/                # builder(LangGraph 图+MathAgent门面) / router(意图) / nodes / react
│   ├── memory/               # short_term / long_term / compressor(分层压缩)
│   ├── tools/                # 11 个注册工具（registry 统一调度+防重）
│   ├── analysis.py  embeddings.py  vectorstore.py  llm.py  metrics.py  state.py
├── web/index.html            # 前端（token 鉴权 + SSE 流式）
└── tests/                    # pytest 冒烟
```

---

## 三、数据流水线（构建期，一次性）

```
GitHub(经 hf-mirror) 下载 TAL-SCQ5K（5000 条）
        │
        ▼
scripts/build_question_bank.py
  ├─ clean_text / latex_to_text：100+ 种 LaTeX 命令映射（\bot→⊥、\sim→∽、
  │   \overrightarrow{AB}→AB…），残留扫描为 0
  ├─ HIGH_SCHOOL 正则过滤超纲（数列/对数/奇偶函数…）
  ├─ TOPIC_RULES 归类：函数 / 二元一次方程 / 几何图像 / 综合
  └─ 合并 scripts/gen_geometry_questions.py 生成的 39 个参数化模板（373 题，
      干扰项程序计算并手算验证）
        │
        ▼
SQLite questions 表（2009 条派生记录，板块库存 572/553/515/369 均 ≥200）
        +
Qdrant 本地嵌入式（BGE-small-zh-v1.5 语义向量，无 torch 自动降级 hashing）
  ├─ knowledge_base          全局题库（只读检索）
  └─ user_long_term_memory   用户私有（检索强制注入 user_id，缺失抛 IsolationError）
```

改动数据后的重建顺序：

```bash
python scripts/gen_geometry_questions.py --per-template 10
python scripts/build_question_bank.py --with-vector
```

---

## 四、核心对话流程（运行期主干）

```
用户消息
   │  POST /api/chat 或 GET /api/chat/stream(SSE) 或 POST /api/upload
   ▼
【鉴权】/api/session 签发 HMAC token → resolve_user：
   带 token → 身份以 token 为准（自报 user_id 被忽略）；require_token=true 时无 token 401
   │
   ▼
【Agent 池】_agent(user_id)：TTL 30min / 容量 100 LRU 淘汰（compressor 已持久化，可安全重建）
   │
   ▼
MathAgent.invoke（per-user RLock，同用户并发串行化）
   ├─ ① prune_state：从上一轮 last_state 取回跨轮键 → 裁剪 → 显式回注 payload
   │     （tool_call_history 旧记录压为 name+args_hash+ok 且条数 ≤24；
   │      observations 滑窗 8；已交卷会话释放 questions 留 records）
   ├─ ② conversation_digest = compressor.build_context() 注入 state
   │     （实体锚永不释放 + 近期原文 6 轮 + 历史要点 ≤12 行 + 闲聊整轮释放，预算 1600 字符）
   ▼
【意图路由 router.py】规则优先、LLM 兜底，优先级：
   空输入 → 学习模式内(概念咨询→chat / 其余→learn) → 概念咨询(什么是/怎么求…)
   → 刷题中短输入(A-D/纯数字)即作答 → 交卷/报告 → 批改 → 习惯 → 学习(我要学/教我/开课)
   → 刷题 → 兜底(刷题中默认继续作答/闲聊)
   │
   ├─ quiz ────────► quiz_manager：fetch_quiz 拉题 → 逐题作答(check_answer 选项下标归一化判分)
   │                  →「交卷」submit → error_analyzer
   ├─ grade ───────► grading：ocr_document(白名单 data/uploads/ + 扩展名) → grade_assignment
   │                  → error_analyzer
   ├─ submit ──────► error_analyzer：analyze_errors 错因分类(概念不清/计算错误/审题偏差/方法缺失)
   │                  → generate_error_report → memory_manager 写长期记忆
   ├─ habit ───────► habit：user_history + extract_habits 学情报告
   ├─ learn ───────► knowledge_tutor（知识点讲解 Agent）：
   │                 选板块 → teaching.CURRICULUM 取知识点 → 讲解(骨架代码层 + LLM 补充段)
   │                 → 例题 → 小测 check_answer → 讲评 →「继续/没听懂/换个例题/退出学习」
   └─ chat ────────► ReAct 循环（react.py）：
         每轮 Thought → Action → Observation
         终止硬约束：loop_count ≥ 8 或信息增益 ≥ 0.80 → finalize
         工具防重：tool_name+args_hash 命中 → 拒绝并回注自我纠正提示
         失败 Fallback：工具连续出错走规则模板
         digest 注入三个 prompt（Thought/Action/Answer）→ 多轮"记得"上下文
   │
   ▼
finalize → 输出审核（safety.sanitize_output 脱敏+防隐私索取）→ 回写分层记忆（user/assistant 各一轮，已脱敏）
   → trace 记录（trace.py 环形缓冲，/api/traces 与 /trace 面板可查）
   → metrics 记录（意图/循环终止原因/工具成败/压缩释放量）→ 返回 response
```

**11 个注册工具**（registry 统一记录 tool_call_history，完整清单见《项目清单-工具路由节点.md》）：

| 工具 | 用途 |
|---|---|
| fetch_quiz / check_answer | 拉题 / 判分（选择题先映射选项下标再比较） |
| grade_assignment / grade_application_question | 整卷批改 / 应用题批改 |
| ocr_document | OCR（后端链：paddleocr → PDF 文本层 → 多模态 LLM → 纯文本） |
| search_knowledge / search_user_memory / update_long_term_memory | 题库检索 / 长期记忆读写（隔离） |
| calculator | 表达式求值 |
| generate_error_report | 错题报告 |
| explain_knowledge_point | 知识点讲解（定义/要点/易错点/口诀 + 例题） |

---

## 五、记忆体系（三层）

1. **分层对话压缩**（memory/compressor.py，每用户一份、经 ShortTermMemory 持久化）：
   - **实体锚**：板块/概念/题目 ID，永不释放，LRU ≤10；
   - **近期原文**：最近 6 轮逐字保留；
   - **历史要点**：滑出窗口但相关的旧轮压成一行要点，≤12 条；
   - **释放**：无实体且非任务性（quiz/submit/grade/habit/learn）的闲聊，滑出即整轮丢弃。
2. **state 跨轮瘦身**：LangGraph 真正的跨轮状态在 checkpointer 里——每轮必须
   「从 last_state 取出 → 裁剪 → 显式回注 payload」，否则裁剪无效（关键坑）。
3. **长期记忆**（Qdrant user_long_term_memory）：错题报告落库，检索强制 user_id 隔离。

---

## 六、服务层（FastAPI, 127.0.0.1:8787）

| 端点 | 说明 |
|---|---|
| `POST /api/session` | user_id 换 HMAC token（30 天有效） |
| `POST /api/chat` | 同步对话 |
| `GET /api/chat/stream` | SSE 流式（trace → 逐字 → done） |
| `POST /api/upload` | 上传作业（文件名净化 + 10MB 上限 + token 鉴权）→ 自动 OCR 批改 |
| `GET /api/stats?token=` | 题库统计 + 本人做题数/正确率 |
| `GET /api/metrics` | 循环终止分布 / 工具错误率 / 意图分布 / 压缩释放量 |
| `GET /api/health` | 轻量健康检查（不触发 BGE 加载） |

安全配置（config.yaml `security:`）：`require_token`（公网必须 true）、`cors_origins`、
`max_upload_mb`、`agent_ttl/agent_max`。SQLite 已开 WAL + busy_timeout=30s，
record_attempt 重试 3 次、失败记日志不再静默丢。

---

## 七、验收体系（五套，改路由/判分/记忆/安全后必跑）

```bash
export PYTHONPATH="D:/游戏库/智能助教/src"
python scripts/eval_optimizations.py   # 34/34  四项优化 + 判分准确性回归
python scripts/eval_conversation.py    # 7/7    34 条意图路由 + 行为约束
python scripts/eval_memory.py          # 21/21  多轮压缩/释放/state 有界
python scripts/eval_security.py        # 16/16  OCR 白名单/鉴权/上传/并发/WAL
python -m pytest tests/ -q             # 7 passed
python scripts/run_cli.py --demo       # 端到端演示（刷题→作答→交卷→报告→习惯）
```

启动：

```bash
cd "D:/游戏库/智能助教"
PYTHONPATH="D:/游戏库/智能助教/src" python -m math_agent.api.server
# 浏览器打开 http://127.0.0.1:8787
```

---

## 八、易踩坑速查（详见 .workbuddy/memory/MEMORY.md）

- 跨轮状态在 checkpointer，payload 是新 dict——不显式回注等于没裁剪；
- 选择题判分必须先映射选项下标；选项前缀正则要严格（标号后须有分隔符）；
- 模板题系数用 `_coef/_kx/_lin`，干扰项必须手算验证；
- Git Bash heredoc 吃反斜杠，Python 正则用 `chr(92)` 构造；
- 同一文件多处 Edit 不要并行批量提交，逐个改+逐个验证；
- `_P_CONCEPT` 不能加"解析/提示"（会挡住刷题中的单题反馈）。
