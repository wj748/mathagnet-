# 启动流程（智能助教 · 初中数学辅导 Agent）

> 更新日期：2026-09-11 ｜ 端口 8787 ｜ Windows / Python 3.13

---

## 0. 启动顺序总览

```
首次启动：依赖安装 → .env 配置 → 题库构建 → 模型就位 → 启动服务 → 健康验证
日常启动：启动服务 → 健康验证                                    （约 10 秒）
```

**依赖关系**：Qdrant 向量库要求**单进程独占**——启动服务前不能有其他进程占用 `data/qdrant`；跑评测前必须先停服务。

---

## 1. 日常启动（题库已建好，99% 的场景）

```bash
cd D:\游戏库\智能助教
set PYTHONPATH=src        # Git Bash 用: export PYTHONPATH="D:/游戏库/智能助教/src"
python -m math_agent.api.server
```

或双击项目根目录的 **`start.bat`**（自动设置 PYTHONPATH 与 venv 路径，并自动拉起 Redis 短期记忆缓存——未在跑才启动，已在跑则跳过）。

**启动后验证**（另开一个终端）：

```bash
curl http://127.0.0.1:8787/api/health
# 期望：{"ok":true,"llm":true|false,"embedder":"bge-small-zh-v1.5"|..., ...}
```

- `llm:false` 不是错误——只是未配 API Key，自动走离线规则模式，全功能可用
- `embedder:"not_loaded_yet"` 也不是错误——后台预热线程正在加载，首个检索请求会稍慢

浏览器打开 **http://127.0.0.1:8787** 即可使用。

---

## 2. 首次启动（新机器 / 全新环境）

| 步骤 | 命令 | 说明 |
|---|---|---|
| ① 安装依赖 | `pip install -r requirements.lock.txt` | 锁定 108 项版本；torch 必须单独装：`pip install --index-url https://download.pytorch.org/whl/cpu torch`（不能写进 requirements 行内） |
| ② 配置 LLM（可选） | 复制 `.env.example` 为 `.env`，填 `GLM_API_KEY` | 不填则离线规则模式，功能完整、表达较固定 |
| ③ 初始化题库+向量库 | `python scripts/init_all.py --skip-eval` | 一键完成：下载题库 → ETL 入库 → 构建向量库（也可分步：`gen_geometry_questions.py` → `build_question_bank.py --with-vector`） |
| ④ 启动服务 | `python -m math_agent.api.server` | 首次启动后台会拉取/加载 BGE 模型到 `models/` |
| ⑤ 健康验证 | `curl /api/health` + 浏览器打开首页 | 见上 |

> 国内网络注意：题库源走 hf-mirror.com 镜像（`.env` 里 `HF_ENDPOINT`），直连 GitHub/ModelScope 不稳。

---

## 3. 容器启动（可选）

```bash
docker build -t math-agent .
docker run -p 8787:8787 --env-file .env -v D:\游戏库\智能助教\data:/app/data math-agent
# 或与 Redis 一起：docker compose --profile full up -d
```

容器内已设 `HOST=0.0.0.0`；挂载 `data/` 以持久化题库与向量库。

---

## 4. 停止服务

- 前台运行：`Ctrl + C`
- 后台运行：任务管理器结束 python 进程，或 `netstat -ano | findstr :8787` 找 PID 后 `taskkill /PID <pid>`

**⚠️ 停止后才能跑评测**（`scripts/eval_*.py`、pytest）——否则 Qdrant 报
`Storage folder data/qdrant is already accessed by another instance`，会被误判为代码回归。

---

## 5. 常见启动问题

| 现象 | 原因 | 处理 |
|---|---|---|
| `Storage folder ... already accessed` | Qdrant 被另一进程占用 | 找到并停掉占用的 python 进程再启动 |
| 端口 8787 被占 | 旧实例未退 | `netstat -ano | findstr :8787` → taskkill |
| `math_agent` 导入失败 | PYTHONPATH 未设 | 必须 `PYTHONPATH=src`（venv 的 python 也要带） |
| 首次请求特别慢 | BGE 模型冷加载（数十秒） | 正常，服务启动时后台自动预热；后续秒级 |
| `embedder: hashing` | 未装 torch | 装上 CPU 版 torch 后重启（语义质量提升，链路不中断） |
| stats 显示"服务未启动" | 前端早于服务就绪 | 刷新页面即可 |
| Redis 连不上 | 服务端未启动（`tools/redis/redis-server.exe`）或客户端版本不对 | 正常降级内存短期记忆，无需处理；要启用缓存：跑 start.bat 或手动启动 Redis 服务端，客户端必须装 **4.x**：`pip install "redis==4.6.0"`（8.x 的 RESP3 握手与 Redis 5 服务端不兼容，报 unknown command HELLO） |

---

## 6. 启动自检清单（服务跑起来了但不确定对不对）

1. `GET /api/health` → `ok:true`
2. 首页正常打开，状态区显示「AI 老师在线」或「基础模式（离线）」
3. 发一句「我要刷函数的选择题」→ 能出题、`trace` 面板显示 `intent=quiz`
4. `/api/metrics` 可访问（公网部署需 token）

以上 4 条通过即可对外使用。
